# Lake Balaton Thermal Monitoring Decision Register

## Authority and use

This is the authoritative, versioned register for scientific, methodological, governance, and scope decisions in this project.

- Valid statuses are **proposed**, **approved**, **rejected**, and **superseded**.
- A chat-only proposal is not approved until the user explicitly approves it and the approval is recorded here.
- When decisions conflict, the newest approved, non-superseded decision controls.
- Material scientific or scope decisions and changes still require explicit user approval through the main coordinator.
- A superseded entry remains in this register and identifies the decision that replaced it.

## Approved decisions

### GOV-001 — Decision authority and agent roles

- **Date:** 2026-08-13
- **Status:** approved
- **Decision:** The main Codex task is the coordinator and the only interface for user approval. Scientific research and independent validation are read-only. Only the GEE application engineer may edit implementation files, and only when the coordinator assigns those files under an approved specification. No agent may commit, push, publish, deploy, create Earth Engine assets, or materially expand scope without explicit user authorization.
- **Rationale:** Centralized approval and separated research, implementation, and validation responsibilities reduce silent scope changes and conflicts.
- **Evidence or source:** Attached “Thesis Methodology Guidance” conversation as summarized in `PROJECT_CONTEXT.md`; `AGENTS.md`; explicit user governance approval on 2026-08-13.
- **Approval provenance:** Explicitly approved by the user through the main coordinator on 2026-08-13; strengthened governance wording explicitly approved in the current task.
- **Affected files or components:** `AGENTS.md`, `PROJECT_CONTEXT.md`, `DECISIONS.md`, `.codex/config.toml`, `.codex/agents/*.toml`, all future implementation and deployment work.
- **Superseded decision:** None.

### GOV-002 — Decision-register authority

- **Date:** 2026-08-13
- **Status:** approved
- **Decision:** `DECISIONS.md` is the authoritative versioned decision register. Approved scientific and scope decisions must be recorded here; chat-only proposals are not approved; and the newest approved, non-superseded decision controls.
- **Rationale:** A durable repository record makes approval provenance reviewable across tasks and prevents proposals or stale chat context from being mistaken for approved rules.
- **Evidence or source:** Explicit user instruction in the current task.
- **Approval provenance:** Explicitly approved by the user through the main coordinator on 2026-08-13.
- **Affected files or components:** `DECISIONS.md`, `AGENTS.md`, all scientific specifications and implementation tasks.
- **Superseded decision:** None.

### SCI-001 — Project purpose and scientific contribution

- **Date:** 2026-08-13
- **Status:** approved
- **Decision:** The internship will develop a Google Earth Engine application for satellite-derived surface-water-temperature anomalies and unusually warm observations in Lake Balaton. It must extend rather than merely reproduce Li, Somogyi, and Tóth (2024), with multi-sensor anomaly monitoring, basin comparison, day/night context, explicit data-quality reporting, and high-resolution inspection.
- **Rationale:** The project needs a distinct contemporary monitoring contribution rather than only a longer retrospective archive.
- **Evidence or source:** Attached conversation as summarized in `PROJECT_CONTEXT.md`, “Purpose and contribution.”
- **Approval provenance:** Recorded in `PROJECT_CONTEXT.md` as established scope approved by the user on 2026-08-13; reaffirmed for registration in the current task.
- **Affected files or components:** Scientific methodology, GEE application requirements, reports, presentation, user guidance.
- **Superseded decision:** None.

### SCI-002 — Core comparison principle

- **Date:** 2026-08-13
- **Status:** approved
- **Decision:** An observation is evaluated against historical conditions for the same spatial unit, seasonal period, satellite, and observation stream.
- **Rationale:** This avoids mixing observations with different spatial, seasonal, platform, or day/night characteristics.
- **Evidence or source:** `PROJECT_CONTEXT.md`, core question and established analytical principles.
- **Approval provenance:** Recorded in `PROJECT_CONTEXT.md` as established methodology approved by the user on 2026-08-13; reaffirmed for registration in the current task.
- **Affected files or components:** Climatology, anomaly, percentile, classification, daily and monthly summaries.
- **Superseded decision:** None.

### DATA-001 — MODIS datasets and separate observation streams

- **Date:** 2026-08-13
- **Status:** approved
- **Decision:** Use Terra MODIS `MODIS/061/MOD11A1` for daily morning and nighttime lake-surface-temperature observations and Aqua MODIS `MODIS/061/MYD11A1` for daily afternoon and nighttime observations. Aqua daytime is the primary candidate for afternoon warm extremes. Terra daytime, Aqua daytime, Terra nighttime, and Aqua nighttime are separate streams; each is compared only with its matching historical stream and they are not directly averaged. Any combined indicator requires a separately proposed and approved method.
- **Rationale:** Platform and day/night streams have distinct observation timing and characteristics and must not be conflated.
- **Evidence or source:** `PROJECT_CONTEXT.md`, “Established datasets and roles.”
- **Approval provenance:** Recorded in `PROJECT_CONTEXT.md` as an established dataset and sensor-role decision approved by the user on 2026-08-13; reaffirmed for registration in the current task.
- **Affected files or components:** MODIS ingestion, preprocessing, climatologies, anomaly calculations, summaries, interface labels, downloads.
- **Superseded decision:** None.

### DATA-002 — Landsat role

- **Date:** 2026-08-13
- **Status:** approved
- **Decision:** Landsat 8/9 Collection 2 Level 2 is used for detailed hotspot inspection on selected clear-sky dates. Its surface-temperature product is delivered on a 30 m grid, but its thermal information must not be described as an independent native 30 m measurement.
- **Rationale:** Landsat adds spatial detail while requiring accurate communication of the thermal product’s effective information content.
- **Evidence or source:** `PROJECT_CONTEXT.md`, “Established datasets and roles.”
- **Approval provenance:** Recorded in `PROJECT_CONTEXT.md` as an established dataset-role decision approved by the user on 2026-08-13; reaffirmed for registration in the current task.
- **Affected files or components:** Landsat preprocessing, hotspot maps, documentation, interface wording.
- **Superseded decision:** None.

### DATA-003 — ERA5-Land role

- **Date:** 2026-08-13
- **Status:** approved
- **Decision:** ERA5-Land daily aggregates may provide weather context such as air temperature, wind, and solar radiation, but reanalysis is contextual and must not replace observed satellite lake-surface temperature.
- **Rationale:** Meteorological context can help interpret observations without being misrepresented as the lake-surface-temperature observation itself.
- **Evidence or source:** `PROJECT_CONTEXT.md`, “Established datasets and roles.”
- **Approval provenance:** Recorded in `PROJECT_CONTEXT.md` as an established dataset-role decision approved by the user on 2026-08-13; reaffirmed for registration in the current task.
- **Affected files or components:** Contextual data processing, charts, interface, downloads, documentation.
- **Superseded decision:** None.

### DATA-004 — Optional in-situ validation role

- **Date:** 2026-08-13
- **Status:** approved
- **Decision:** In-situ measurements, if obtained, are optional validation data. Their measurement depth and timing may not match satellite skin temperature or satellite overpass time and must be interpreted accordingly.
- **Rationale:** Validation must acknowledge representativeness differences between field and satellite observations.
- **Evidence or source:** `PROJECT_CONTEXT.md`, “Established datasets and roles.”
- **Approval provenance:** Recorded in `PROJECT_CONTEXT.md` as an established validation-role decision approved by the user on 2026-08-13; reaffirmed for registration in the current task.
- **Affected files or components:** Validation methodology, reports, uncertainty statements.
- **Superseded decision:** None.

### TIME-001 — Historical, monitoring, and archive periods

- **Date:** 2026-08-13
- **Status:** approved
- **Decision:** The historical reference period is 1 January 2003 through 31 December 2022; monitoring begins 1 January 2023 and continues through the latest valid available observation; the user-explorable archive begins in 2003. The reference starts in 2003 because it is the first complete calendar year shared by Terra and Aqua.
- **Rationale:** These periods provide a shared complete Terra/Aqua baseline and a distinct monitoring era.
- **Evidence or source:** `PROJECT_CONTEXT.md`, “Established periods.”
- **Approval provenance:** Recorded in `PROJECT_CONTEXT.md` as established periods approved by the user on 2026-08-13; reaffirmed for registration in the current task.
- **Affected files or components:** Dataset filters, climatology, monitoring views, metadata, validation.
- **Superseded decision:** None.

### QA-001 — Missing observations and data-quality reporting

- **Date:** 2026-08-13
- **Status:** approved
- **Decision:** Do not invent or interpolate a satellite observation solely because cloud or QA masking leaves a date without valid data. Report valid-water coverage and observation counts, and provide an explicit no-valid-observation state when appropriate.
- **Rationale:** Users must be able to distinguish observed values from missing data and judge reliability.
- **Evidence or source:** `PROJECT_CONTEXT.md`, “Established analytical principles” and “Daily observation view.”
- **Approval provenance:** Recorded in `PROJECT_CONTEXT.md` as established analytical and output requirements approved by the user on 2026-08-13; reaffirmed for registration in the current task.
- **Affected files or components:** QA masking, daily and monthly summaries, interface states, downloads, validation.
- **Superseded decision:** None.

### TERM-001 — Thermal-event terminology

- **Date:** 2026-08-13
- **Status:** approved
- **Decision:** Use terms such as “unusually warm observation,” “extreme warm observation,” or “observed extreme thermal episode” unless a validated method supports a stronger term. Do not claim a formal lake heatwave without an approved, scientifically supported method for consecutive exceedances and missing daily observations. Hobday et al. (2016) is a scientific starting point, not an approved operational method.
- **Rationale:** Terminology must not imply event persistence or formal heatwave detection that the approved method has not established.
- **Evidence or source:** `PROJECT_CONTEXT.md`, “Established analytical principles”; Hobday et al. (2016), https://doi.org/10.1016/j.pocean.2015.12.014.
- **Approval provenance:** Recorded in `PROJECT_CONTEXT.md` as established terminology constraints approved by the user on 2026-08-13; reaffirmed for registration in the current task.
- **Affected files or components:** Classification labels, interface text, reports, presentation, downloads.
- **Superseded decision:** None.

### OUT-001 — Required output categories

- **Date:** 2026-08-13
- **Status:** approved
- **Decision:** The intended product includes daily observation, monthly summary, detailed Landsat hotspot, approved spatial comparison, ERA5-Land context, and downloadable summary views with the fields described in `PROJECT_CONTEXT.md`. Exact definitions and interface layout remain unresolved where they depend on proposed methodological decisions.
- **Rationale:** These categories define the intended monitoring product while preserving approval gates for analytical definitions and UI details.
- **Evidence or source:** `PROJECT_CONTEXT.md`, “Intended outputs.”
- **Approval provenance:** Recorded in `PROJECT_CONTEXT.md` as intended product requirements approved by the user on 2026-08-13; reaffirmed for registration in the current task.
- **Affected files or components:** Interface, summaries, maps, charts, tables, downloads, documentation.
- **Superseded decision:** None.

### PERF-001 — Performance architecture principle

- **Date:** 2026-08-13
- **Status:** approved
- **Decision:** The interface must not recompute the full 2003–2022 archive after every user interaction. Processing must filter early by date, sensor, and region and reuse approved precomputed climatologies, percentiles, and basin summaries for expensive historical operations. Asset schema, storage/export destination, refresh process, and deployment remain proposed.
- **Rationale:** Interactive performance requires avoiding repeated archive-wide computation.
- **Evidence or source:** `PROJECT_CONTEXT.md`, “Performance strategy.”
- **Approval provenance:** Recorded in `PROJECT_CONTEXT.md` as an established architecture principle approved by the user on 2026-08-13; reaffirmed for registration in the current task.
- **Affected files or components:** Data architecture, callbacks, precomputation, exports, deployment planning.
- **Superseded decision:** None.

### SCOPE-001 — Thesis–internship separation

- **Date:** 2026-08-13
- **Status:** approved
- **Decision:** The MSc thesis concerns historical 1978–1989 Lake Balaton sediment chemistry, spatial autocorrelation, anisotropy, variography, and interpolation. The internship concerns contemporary satellite-derived lake-surface-temperature monitoring and GEE application development. The internship must not claim that current thermal observations explain historical sediment concentrations, directly correlate the mismatched periods as contemporaneous, or present historical sediment conditions as current.
- **Rationale:** The workstreams use different phenomena and time periods and must remain scientifically distinct.
- **Evidence or source:** `PROJECT_CONTEXT.md`, “Thesis–internship separation.”
- **Approval provenance:** Recorded in `PROJECT_CONTEXT.md` as an established scope boundary approved by the user on 2026-08-13; reaffirmed for registration in the current task.
- **Affected files or components:** Scientific scope, analysis, reports, presentation, application narrative.
- **Superseded decision:** None.

### SCOPE-002 — Six-week delivery sequence

- **Date:** 2026-08-13
- **Status:** approved
- **Decision:** Follow the six-week sequence in `PROJECT_CONTEXT.md`: audit the prior study/application; establish approved preprocessing and QA; implement approved analytical methods; build approved comparisons and outputs; validate; and finalize the application and documentation. This sequence does not approve any unresolved methodology.
- **Rationale:** The sequence organizes delivery while retaining scientific approval gates.
- **Evidence or source:** `PROJECT_CONTEXT.md`, “Six-week delivery frame.”
- **Approval provenance:** Recorded in `PROJECT_CONTEXT.md` as the established delivery frame approved by the user on 2026-08-13; reaffirmed for registration in the current task.
- **Affected files or components:** Project planning, research, implementation, validation, documentation, presentation.
- **Superseded decision:** None.

### SCOPE-004 — Academic approval of broad internship topic

- **Date:** 2026-08-12
- **Status:** approved
- **Decision:** Academic supervisor Dr. Pál Márton approved the broad internship topic as suitable. The approved concept is a GEE application extending the existing Lake Balaton application with thermal-status or anomaly interpretation, daily observations, and basin-level monthly summaries. This academic approval does not approve all detailed methodological choices represented by the 13 proposed decisions in this register. The scope may be refined during the proposed consultation at the beginning of September 2026.
- **Rationale:** The supervisor confirmed that the proposed topic is suitable for completing the internship while leaving detailed methodology and possible scope refinement for subsequent consultation and approval.
- **Evidence or source:** User-provided correspondence evidence: proposal sent on 28 July 2026; supervisor response dated 12 August 2026 stating, “The topic looks promising! It is suitable for completing the internship.”
- **Approval provenance:** Academic suitability approval provided by Dr. Pál Márton on 12 August 2026 and supplied by the user for inclusion in the project record. This is distinct from project approval of detailed methodological choices.
- **Affected files or components:** Broad internship scope, project documentation, consultation planning, and the conceptual requirements for thermal-status or anomaly interpretation, daily observations, and basin-level monthly summaries. It does not authorize implementation of unresolved methodological choices.
- **Superseded decision:** None.

### PROTO-001 — First MODIS preprocessing diagnostic prototype specification

- **Date:** 2026-08-16
- **Status:** approved
- **Decision:** Approve `docs/FIRST_PROTOTYPE_SPECIFICATION.md` as the controlling task specification for the first implementation prototype. The prototype is non-UI, non-deployed, creates no permanent Earth Engine assets or exports, and covers UTC product dates from 1 July 2024 inclusive through 15 July 2024 exclusive. It produces one diagnostic row for each date and each of four separate streams: Terra daytime and nighttime from `MODIS/061/MOD11A1`, and Aqua daytime and nighttime from `MODIS/061/MYD11A1`.

  The prototype uses the corresponding LST, QC, view-time, view-angle, and clear-coverage bands; applies documented scale factors; decodes all QC components; calculates QA diagnostics before common-band masking; preserves UTC product date and local-solar view-time statistics; and uses one uniform typed schema for all 56 expected rows.

  The strict prototype acceptance rule requires unmasked LST, QC, view-time, and view-angle values within documented raw ranges and requires mandatory QA, data quality, emissivity error, and LST error codes all to equal zero. `Clear_*_cov` remains an uninterpreted unitless diagnostic and does not gate acceptance.

  The provisional geometry is geodesic `ee.Geometry.Point([17.7498611, 46.8516944]).buffer(250)` with ID `szemesi_station_250m_v0`, normalized from a published Szemesi-basin sampling-station coordinate. It is a presumed-open-water engineering fixture only, not an authoritative lake/basin geometry or a 250 m thermal measurement.

  Eligible ROI area is the fixed pixel-area-weighted geometry intersection in the exact native MODIS projection. `accepted_roi_fraction` is the accepted-area ratio. Provisional eligible-water, valid-water, and coverage aliases may be emitted only with the explicit semantic warning that they are geometry-based engineering proxies and not application-grade water coverage.

  The prototype must preserve accepted, produced-but-rejected, cloud, non-cloud no-retrieval, missing-QC, no-source-image, mixed-failure, and duplicate-source diagnostics. Duplicate source images are a validation failure and must not be mosaicked. Missing observations remain explicit typed-null rows and are never interpolated.

  Climatology, anomalies, percentiles, thermal-status labels, monthly aggregation, authoritative water/basin geometries, scientific coverage thresholds, permanent assets, Landsat, ERA5-Land, deployment, and UI remain outside the prototype. This decision does not resolve any of the 13 application-level proposed decisions.
- **Rationale:** A small, auditable diagnostic prototype is needed to validate collection access, stream parameterization, scaling, QA decoding, mask behavior, timing metadata, area accounting, missingness, state representation, and schema consistency before broader scientific methodology or application implementation is approved.
- **Evidence or source:** `PROJECT_CONTEXT.md`; `DECISIONS.md`; Phase 1 scientific and technical audit; official Earth Engine MOD11A1.061 and MYD11A1.061 documentation; MOD11 Collection 6.1 User Guide; Blix et al. (2018) station table; independent scientific validation dated 2026-08-16; `docs/FIRST_PROTOTYPE_SPECIFICATION.md`.
- **Approval provenance:** Explicitly approved by the user through the main coordinator on 2026-08-16.
- **Affected files or components:** `docs/FIRST_PROTOTYPE_SPECIFICATION.md` and the first prototype implementation and tests explicitly assigned by the coordinator. No UI, deployment, Earth Engine assets, exports, climatology, anomaly, percentile, classification, monthly, Landsat, or ERA5-Land implementation is authorized.
- **Superseded decision:** None.

### PROTO-002 — Sparse null-by-omission diagnostic schema

- **Date:** 2026-08-17
- **Status:** approved
- **Decision:** For the first MODIS diagnostic prototype, the ordered schema manifest remains authoritative for every logical output field, its type, nullability, and order. An unavailable nullable value is omitted from the evaluated Earth Engine `Feature`; absence of the property means unavailable/null. Numeric and string sentinel values are forbidden. Physical property sets may differ while every row must conform to the same logical schema. Any future tabular export must use an explicit ordered selector list derived from the authoritative schema manifest.
- **Rationale:** Authenticated Earth Engine execution confirmed that properties assigned null are omitted from evaluated `Feature.propertyNames()`. Sparse null-by-omission preserves explicit missingness without inventing sentinel values while retaining a consistent, testable logical contract.
- **Evidence or source:** Authenticated Earth Engine execution of the approved prototype on 2026-08-17; user-provided Console results showing different physical property sets where evaluated values were null; explicit user approval of Option A on 2026-08-17.
- **Approval provenance:** Explicitly approved by the user through the main coordinator on 2026-08-17.
- **Affected files or components:** `docs/FIRST_PROTOTYPE_SPECIFICATION.md`, `src/prototype/modis_preprocessing_prototype.js`, `src/prototype/modis_preprocessing_tests.js`, `docs/PROTOTYPE_EXECUTION_GUIDE.md`, and any future tabular export selector construction for this prototype.
- **Superseded decision:** Supersedes only the incompatible PROTO-001 requirements that nullable properties remain physically present with null values and that every evaluated Earth Engine row have an identical physical property set. All other requirements of PROTO-001 remain approved and controlling.

### AUDIT-001 — Whole-lake boundary and shoreline sensitivity audit

- **Date:** 2026-08-18
- **Status:** approved
- **Decision:** Approve a read-only, non-UI sensitivity audit of the WISE WFD v1.9 Lake Balaton surface-water-body polygon identified by `thematicIdIdentifier = HUAIH049` and `cYear = 2022`. The source must be freshly retrieved from the approved exact query and must fail closed if its redirect behavior, HTTP metadata, decompressed byte count, SHA-256, feature count, geometry type, ring/coordinate count, identifier metadata, or reported size differ from the recorded source. Source coordinates are transient and must not be stored in the repository or in an Earth Engine asset. In EPSG:3035, compare inward shoreline treatments of 0 m, 463.31271656937485 m, 500 m, and 926.6254331387497 m over four approved 14-day 2024 windows (15–29 January, April, July, and October), retaining Terra day, Terra night, Aqua day, and Aqua night separately and reusing the strict `PROTO-001`/`PROTO-002` diagnostic processor. The complete audit must contain exactly 896 date-stream-treatment rows and exactly 80 coverage summaries, use deterministic chunks, Type-7 quantiles and population standard deviation, and report continuous coverage distributions with descriptive 5%, 25%, and 50% markers. It must not automatically select or approve a final lake boundary, erosion distance, or coverage threshold.
- **Rationale:** The application needs reproducible evidence about whole-lake boundary provenance, shoreline mixed-pixel sensitivity, seasonal/stream coverage, and treatment consequences before resolving `SPACE-001` or `QA-002`.
- **Evidence or source:** WISE WFD v1.9 September 2025 public service, layer `SurfaceWaterBody_polygon`; prior retrieval evidence for `HUAIH049`; approved first MODIS diagnostic prototype and its validation report; independent research and scientific review completed before approval.
- **Approval provenance:** Explicitly approved by the user through the main coordinator on 2026-08-18, including the exact authorized file scope.
- **Affected files or components:** `requirements-geometry-audit.txt`, `docs/WHOLE_LAKE_BOUNDARY_SHORELINE_AUDIT_SPECIFICATION.md`, `docs/WHOLE_LAKE_BOUNDARY_SHORELINE_AUDIT_EXECUTION_GUIDE.md`, `docs/WHOLE_LAKE_BOUNDARY_SHORELINE_AUDIT_REPORT.md`, `src/audit/whole_lake_boundary_shoreline_audit.js`, `src/audit/whole_lake_boundary_shoreline_tests.js`, and `tools/verify_wise_huaih049.py`. This authorizes audit code and evidence only; no application UI, Earth Engine asset, export, deployment, or production geometry is approved.
- **Superseded decision:** None. This decision does not resolve or supersede `SPACE-001`, `QA-002`, or `SCOPE-003`.

### AUDIT-002 — WISE source-representation geometry sensitivity audit

- **Date:** 2026-08-18
- **Status:** approved
- **Decision:** Authorize a deterministic, read-only geometry sensitivity audit of three exact WISE representations of `HUAIH049`: the immutable original Hungarian 2022 CDR GML, the exact unrounded WISE service response, and the exact seven-decimal service response retained as invalid evidence. Authoritative replacements must be tested before any geometric repair is considered. The original source bytes and geometries remain immutable, and raw coordinates, GeoJSON, and GML must not be persisted in the repository. The audit may compare exact source identity and hashes, CRS/axis order, validity, topology, parts, holes, vertices, area, perimeter, symmetric difference, Hausdorff distance, fragments, overlaps, and visual evidence. Geometry-repair operations are permitted only in a separately approved fallback if no valid authoritative representation exists; because valid authoritative representations have been identified, this audit must not invoke them. Automated unexpected-land classification is not authorized without an independent land mask. This decision authorizes testing and derived evidence only; it does not select a production geometry, resolve `SPACE-001`, authorize the 896-row Earth Engine audit, or authorize Earth Engine assets, exports, deployment, or UI code.
- **Rationale:** The exact seven-decimal service response is invalid because coordinate rounding creates a self-intersection, while the original CDR GML and unrounded service response provide valid authoritative alternatives that must be compared without modifying source geometry.
- **Evidence or source:** EEA CDR metadata index; original `SurfaceWaterBody_HU_20220322.gml`; exact WISE WFD service responses with and without `geometryPrecision=7`; independent research and explicit user approval communicated through the main coordinator on 2026-08-18.
- **Approval provenance:** Explicitly approved by the user through the main coordinator on 2026-08-18 for the exact testing-only file scope.
- **Affected files or components:** `requirements-geometry-audit.txt`, `tools/analyze_wise_huaih049_source_representations.py`, `tools/test_analyze_wise_huaih049_source_representations.py`, `docs/WHOLE_LAKE_GEOMETRY_REPAIR_SENSITIVITY_REPORT.md`, `docs/WHOLE_LAKE_GEOMETRY_REPAIR_SENSITIVITY_SOURCE_RECORD.json`, and the three approved derived PNG figures under `docs/figures/`. No prior `AUDIT-001` implementation or specification is modified.
- **Superseded decision:** None. This decision does not supersede or resolve `AUDIT-001`, `SPACE-001`, `QA-002`, or `SCOPE-003`.

### AUDIT-003 — Unrounded WISE polygon for AUDIT-001 operational testing

- **Date:** 2026-08-18
- **Status:** approved
- **Decision:** Use the exact unrounded WISE WFD service polygon for `HUAIH049`, reporting year 2022, as the operational geometry for `AUDIT-001` testing only. The request must omit `geometryPrecision`; coordinate rounding is forbidden. The exact request/final URL, zero-redirect behavior, HTTP metadata, 438,617 response bytes, raw SHA-256 `5d5f9c8edfced710fcc8657a6aceb398f14cd815d1c5e41a7df9f00601f312d8`, 427,942 canonical JSON bytes, canonical SHA-256 `394a929617bb975d2f3e3aa627abe26dbcc98f42fa48fe038757b3e734c35020`, identity, properties, one-part/no-hole/11,012-tuple structure, closure, validity, and simplicity must pass fail-closed verification. The original Hungarian EPSG:3035 GML remains the immutable provenance and metric cross-check and must independently pass its exact CDR metadata, byte/hash, identity, CRS/axis-order, validity, structure, area, and perimeter checks. Only verified unrounded coordinates may be emitted transiently to an authenticated scratch editor; they must never be persisted in the repository, an Earth Engine asset, or an export. The invalid seven-decimal response is removed as an operational input but retained in audit history and documentation as the source-gate failure caused by rounding.
- **Rationale:** `AUDIT-002` established that the original GML and unrounded service polygon are valid, simple, authoritative representations with negligible metric differences, while seven-decimal rounding creates a self-intersection.
- **Evidence or source:** Approved `AUDIT-002`; `docs/WHOLE_LAKE_GEOMETRY_REPAIR_SENSITIVITY_REPORT.md`; exact CDR metadata, original GML, and unrounded WISE service evidence recorded on 2026-08-18.
- **Approval provenance:** Explicitly approved by the user through the main coordinator on 2026-08-18.
- **Affected files or components:** The existing `AUDIT-001` verifier, specification, execution guide, report, audit runtime, and tests only. Four treatments, four windows, four streams, 896 diagnostic rows, and 80 summaries remain unchanged.
- **Superseded decision:** Supersedes only the `AUDIT-001` operational use of the invalid seven-decimal WISE service representation. It does not supersede the historical evidence, resolve `SPACE-001`, select a final production boundary or erosion distance, approve a coverage threshold, or authorize Earth Engine assets, exports, deployment, or application UI.

### AUDIT-004 — Local official Earth Engine Python API execution route

- **Date:** 2026-08-20
- **Status:** approved
- **Decision:** The authenticated `AUDIT-001`/`AUDIT-003` execution may use the official local Earth Engine Python API through the user's OAuth authorization because embedding the verified 11,012-tuple geometry and complete runtime in an Earth Engine Code Editor scratch script is operationally infeasible on the available client. The runner must freshly invoke the approved fail-closed WISE verifier, retain verified unrounded `HUAIH049` coordinates in process memory only, initialize Earth Engine with a user-supplied Google Cloud project, and evaluate at most one vector treatment or one deterministic 56-row season-treatment chunk at a time. It must preserve the approved datasets, four separate streams, geometry operations, erosion distances, dates, strict QA, scales, masks, schemas, formulas, Type-7 quantiles, population SD, 896-row/80-summary acceptance gates, and all decision boundaries. OAuth credentials may be stored only by the official Earth Engine authentication flow in its standard local credential location. No Earth Engine asset, export, task, saved source geometry, deployment, or UI is authorized. No final boundary, erosion distance, or coverage threshold is approved, and `SPACE-001` remains proposed.
- **Rationale:** Repeated authenticated Code Editor attempts showed that the large transient geometry/runtime script can freeze or break the browser before completing bounded evaluation. Moving orchestration to the official Python client removes the editor-rendering bottleneck while retaining server-side Earth Engine computation and the approved fail-closed, memory-only source contract.
- **Evidence or source:** Authenticated Code Editor execution history recorded in `docs/WHOLE_LAKE_BOUNDARY_SHORELINE_AUDIT_REPORT.md`; official Earth Engine Python authentication and initialization documentation; explicit user approval communicated through the main coordinator on 2026-08-20.
- **Approval provenance:** Explicitly approved by the user through the main coordinator on 2026-08-20 with the instruction “Approve that python api”.
- **Affected files or components:** `requirements-geometry-audit.txt`, `tools/run_whole_lake_boundary_shoreline_audit_ee.py`, `docs/WHOLE_LAKE_BOUNDARY_SHORELINE_AUDIT_EXECUTION_GUIDE.md`, and `docs/WHOLE_LAKE_BOUNDARY_SHORELINE_AUDIT_REPORT.md`. Existing JavaScript remains scientific-reference and local-test material; it is not changed by this decision.
- **Superseded decision:** Supersedes only the operational requirement in `AUDIT-001`/`AUDIT-003` that authenticated execution use transient Code Editor declarations. It does not supersede any scientific, geometry, provenance, validation, or scope requirement and does not resolve `SPACE-001`, `QA-002`, or `SCOPE-003`.

### AUDIT-005 — Full-grid numerical MODIS affine equivalence for AUDIT-001

- **Date:** 2026-08-20
- **Status:** approved
- **Decision:** For `AUDIT-001` only, source and canonical MODIS projections must have exactly identical CRS identifiers, and all five configured bands within each source image must have exactly identical CRS identifiers and six affine coefficients. Source-versus-canonical affine coefficients are numerically equivalent if and only if the maximum Euclidean displacement induced by their coefficient differences over the four outer corners of the complete 43,200-column by 21,600-row MODIS sinusoidal grid is no greater than 0.0001 m (0.1 mm). Grid coordinates are pixel-grid corner coordinates `(column, row)` at `(0,0)`, `(43200,0)`, `(0,21600)`, and `(43200,21600)` under the Earth Engine six-coefficient mapping `x = a*column + b*row + c`, `y = d*column + e*row + f`. Raw canonical/source affines, elementwise differences, corner displacements, and the maximum displacement must be retained in coordinate-free audit evidence. No reprojection or resampling is permitted. Any CRS mismatch, intra-image band-projection mismatch, malformed metadata, or displacement above 0.1 mm is a hard audit error.
- **Rationale:** Authenticated evidence showed microscopic representation differences in otherwise compatible MODIS affines. Evaluating their maximum induced physical displacement over the entire native grid is stricter and more meaningful than coefficient-by-coefficient tolerance while preventing accumulated scale or shear error from being hidden.
- **Evidence or source:** Authenticated `AUDIT-001` projection diagnostics and explicit user approval communicated through the main coordinator on 2026-08-20.
- **Approval provenance:** Explicitly approved by the user through the main coordinator on 2026-08-20 for `AUDIT-001` only, including the exact 0.1 mm full-grid displacement rule, exact CRS and intra-image band projections, raw evidence retention, no reprojection/resampling, and hard failure above threshold.
- **Affected files or components:** `DECISIONS.md`, `docs/WHOLE_LAKE_BOUNDARY_SHORELINE_AUDIT_SPECIFICATION.md`, `tools/run_whole_lake_boundary_shoreline_audit_ee.py`, `docs/WHOLE_LAKE_BOUNDARY_SHORELINE_AUDIT_EXECUTION_GUIDE.md`, and `docs/WHOLE_LAKE_BOUNDARY_SHORELINE_AUDIT_REPORT.md`. The 100-field row manifest and scientific reducer affine remain unchanged.
- **Superseded decision:** Supersedes only exact source-versus-canonical numerical affine equality for `AUDIT-001`. It does not supersede exact CRS equality, exact configured-band projection equality, any scientific processing rule, `SPACE-001`, `QA-002`, `SCOPE-003`, or any final boundary, shoreline treatment, or coverage-threshold decision.

### AUDIT-006 — Resumable coordinate-free local checkpoint and bounded session

- **Date:** 2026-08-22
- **Status:** approved
- **Decision:** The authenticated `AUDIT-001` execution may persist one private, untracked, coordinate-free local checkpoint at `local_run_state/whole_lake_boundary_shoreline_audit_checkpoint.json`. The runner must stop before a hard 165-minute session limit, use an eight-minute limit and exactly one attempt for each Earth Engine request, and atomically save evidence only after a vector or deterministic 56-row chunk has passed all applicable schema, provenance, type, count, formula, and validation gates. On restart it must resume from the first incomplete compatible chunk and must not recompute completed compatible chunks. Checkpoint compatibility must fail closed against the exact verified source identity and hashes, approved method and implementation versions, request/runtime configuration, date windows, streams, shoreline treatments, and authoritative schemas/manifests. The checkpoint may contain only coordinate-free evaluated evidence needed for complete 896-row/80-summary acceptance; it must contain no raw geometry or coordinates, OAuth credentials or tokens, raster data, Earth Engine assets, exports, tasks, deployments, or UI state. A failed or partially evaluated phase must never be recorded as complete. Atomic checkpoint persistence does not itself claim full audit acceptance; acceptance remains permitted only after compatible validated evidence for all four vectors, all 16 chunks, 896 unique row keys, and 80 summaries passes the existing complete gates.
- **Rationale:** Previous bounded authenticated runs repeatedly lost already validated chunks after later transport or implementation failures. Durable validated-only evidence prevents unnecessary recomputation without weakening the approved audit or mixing incompatible runs.
- **Evidence or source:** Authenticated execution history documented in `docs/WHOLE_LAKE_BOUNDARY_SHORELINE_AUDIT_REPORT.md`; explicit user request to finish within less than three hours; explicit approval on 2026-08-22 of the proposed coordinate-free checkpoint path and 165-minute limit, followed by the clarification that a failure at chunk 6 must resume at chunk 6 while preserving validated chunks 1–5.
- **Approval provenance:** Explicitly approved by the user through the main coordinator on 2026-08-22.
- **Affected files or components:** `.gitignore`, `DECISIONS.md`, `tools/run_whole_lake_boundary_shoreline_audit_ee.py`, `docs/WHOLE_LAKE_BOUNDARY_SHORELINE_AUDIT_EXECUTION_GUIDE.md`, and `docs/WHOLE_LAKE_BOUNDARY_SHORELINE_AUDIT_REPORT.md`. This authorizes only local coordinate-free checkpoint persistence and bounded read-only execution. It does not authorize Earth Engine assets, exports, tasks, deployment, UI code, a production geometry, a shoreline treatment, or a coverage threshold.
- **Superseded decision:** Supersedes only the prior operational statement that the runner writes no checkpoint and the prior 1,200-second/two-attempt request defaults for the resumable full audit. It does not supersede any scientific, provenance, geometry, schema, validation, or acceptance requirement of `AUDIT-001` through `AUDIT-005`.

### AUDIT-007 — Structured UTF-8 Content-Type gate for the original GML

- **Date:** 2026-08-22
- **Status:** approved
- **Decision:** For the immutable original Hungarian 2022 CDR GML used by `AUDIT-003`, parse the HTTP `Content-Type` structurally and require the media type to be exactly `text/xml`, compared case-insensitively. Permit either no media-type parameter or exactly one `charset` parameter whose value is UTF-8, compared case-insensitively. Reject duplicate `charset` parameters, every unknown parameter, and every non-UTF-8 charset. Retain the raw observed `Content-Type` header in coordinate-free audit evidence. Every other source gate remains exact and unchanged, including requested/final URL, zero redirects, HTTP status, empty content encoding, Last-Modified, byte count, SHA-256, CDR row, XML encoding evidence, identity, CRS and axis order, topology, validity, simplicity, area, perimeter, and GML-versus-unrounded-service cross-checks.
- **Rationale:** On 2026-08-22 the authoritative endpoint returned `text/xml; charset=utf-8` instead of the previously observed `text/xml`, while the exact 4,735,017 source bytes, SHA-256 `0604c6c3073dfb06faf98d0bce5b093be834c5de5a9ad78b326b30f094e809f2`, Last-Modified value, URL, redirects, status, encoding, XML UTF-8 declaration, geometry identity, topology, metrics, and all cross-representation checks remained unchanged. HTTP and XML media-type standards define `charset` as an optional parameter for `text/xml`; the observed UTF-8 parameter agrees with the immutable document bytes.
- **Evidence or source:** Live coordinate-free retrieval and complete downstream diagnostic verification on 2026-08-22; RFC 9110 sections 8.3–8.3.2; RFC 7303 sections 8.1, 9.1–9.2; independent read-only thermal remote-sensing research; explicit user approval communicated through the main coordinator.
- **Approval provenance:** Explicitly approved by the user through the main coordinator on 2026-08-22 with the instruction “Approve the narrow UTF-8 Content-Type gate update and resume the audit.”
- **Affected files or components:** `DECISIONS.md`, `tools/verify_wise_huaih049.py`, its applicable local tests, `docs/WHOLE_LAKE_BOUNDARY_SHORELINE_AUDIT_EXECUTION_GUIDE.md`, and `docs/WHOLE_LAKE_BOUNDARY_SHORELINE_AUDIT_REPORT.md`. This authorizes only the structured original-GML media-type gate and authenticated audit resumption. It does not authorize changes to source bytes, geometry, scientific processing, boundary or erosion selection, coverage thresholds, Earth Engine assets, exports, deployment, or application UI.
- **Superseded decision:** Supersedes only the `AUDIT-003` requirement that the original GML raw `Content-Type` header string equal `text/xml`. It does not supersede any other source, provenance, geometry, scientific, validation, checkpoint, or acceptance gate.

### AUDIT-008 — Windows process supervisor for hard request wall-clock limits

- **Date:** 2026-08-23
- **Status:** approved
- **Decision:** Continue the resumable `AUDIT-001` processing from the saved `AUDIT-006` checkpoint only through Windows supervisor implementation `whole_lake_boundary_shoreline_audit_windows_supervisor_v1`, SHA-256 `195ed5051c06a4e950651769bb120e219a84de559f3580268e4a276994a714c8`, around the byte-for-byte unchanged v8 runner. Before launch, the supervisor must fail closed unless it revalidates the exact checkpoint compatibility fingerprint `39c2535a705cfb9aeb9d0b89a6eb35f4f8e3e96a5dce03c3f193257bce18314e`, exact runner SHA-256 `811238f082275eb8c154689016114dc79340b001d7f9d046c577abe81b03c404`, four saved vectors, completed chunk 01, coordinate-free compatibility/evidence, contiguous completed chunks, and `full_acceptance_claimed: false`. For every exact lifecycle-marked Earth Engine request, the supervisor must apply a monotonic 480.000-second wall-clock cutoff, reject completion or payload at or after the cutoff, and terminate the complete child process tree through a non-breakaway Windows Job Object. It must make no automatic retry. It must independently preserve the 165-minute overall session ceiling. Only exact top-level, exact-schema lifecycle records from the pinned runner may control a timer; arbitrary, nested, quoted, malformed, duplicated, unknown, or out-of-sequence marker-like output must not spoof the lifecycle state. The supervisor does not alter the runner, checkpoint contents, request graphs or decomposition, scientific method, schemas, source coordinates, assets, exports, tasks, UI, or deployment.
- **Rationale:** The pinned Earth Engine client passes `ee.data.setDeadline(480000)` to Requests as connect/read socket timeouts, not as an end-to-end wall-clock deadline. A validated chunk request consequently returned after 868.25 seconds. Process isolation supplies the approved true wall-clock cutoff without changing the scientific runner hash or discarding already validated compatible evidence.
- **Evidence or source:** Authenticated v8 execution on 2026-08-22/23; installed `earthengine-api==1.7.37` and Requests transport path; official Requests timeout documentation; the coordinator's narrow external-supervisor proposal; explicit user approval communicated through the main coordinator on 2026-08-23.
- **Approval provenance:** Explicitly approved by the user through the main coordinator on 2026-08-23, after the coordinator presented the narrow versioned external-supervisor conditions and requested approval, with the exact instruction “ok I approve to continue the processing of the next chunks, from where we stop before”.
- **Affected files or components:** `DECISIONS.md`, `tools/supervise_whole_lake_boundary_shoreline_audit.py`, `docs/WHOLE_LAKE_BOUNDARY_SHORELINE_AUDIT_EXECUTION_GUIDE.md`, and `docs/WHOLE_LAKE_BOUNDARY_SHORELINE_AUDIT_REPORT.md`. This authorizes only the fail-closed external execution envelope and reuse of exact compatible validated checkpoint evidence. It does not authorize checkpoint migration or repair, runner changes, altered request decomposition, scientific changes, source-coordinate persistence, Earth Engine assets, exports, tasks, deployment, or UI.
- **Superseded decision:** Supersedes only reliance on the official client's 480-second socket timeout as sufficient enforcement of the `AUDIT-006` per-request wall-clock limit. It does not supersede any checkpoint compatibility, scientific, provenance, geometry, schema, validation, acceptance, or scope requirement of `AUDIT-001` through `AUDIT-007`.

### AUDIT-009 — Order-independent checkpoint stream validation and exact migration

- **Date:** 2026-08-23
- **Status:** approved
- **Decision:** Validate each checkpoint chunk's `coverage_values` object by requiring the exact set of four approved stream names—`terra_day`, `terra_night`, `aqua_day`, and `aqua_night`—independent of JSON object-member order, then reconstruct those values in the canonical `STREAMS` order in memory before all existing value and summary-formula checks. Advance the runner to implementation `whole_lake_boundary_shoreline_audit_python_api_v9_order_independent_checkpoint_streams`, SHA-256 `07ec5a72ffe420bd3954b5f4865eb2ba4b4bd86b8338d783f243f5d0e2ef2863`, and compatibility SHA-256 `ccdebab8bba4983462a9e3134f2411255fbcb08fa3ded98b2dce906dfe403ca9`. Authorize one atomic in-place migration only from checkpoint raw SHA-256 `2c5012b9b133d72a4b5842a73dd81cfc44eebff2f31f715aa4ba2d5f7a091a2d`, prior compatibility SHA-256 `39c2535a705cfb9aeb9d0b89a6eb35f4f8e3e96a5dce03c3f193257bce18314e`, and prior runner SHA-256 `811238f082275eb8c154689016114dc79340b001d7f9d046c577abe81b03c404`. Migration must additionally prove the exact four vectors, sole completed chunk 01, false acceptance flag, exact legacy order-rooted/cascade failure set, exact four-stream semantic mapping, passing v9 validation, unchanged vector/chunk evidence values, unchanged chunk canonical hash and certificate, and changes limited to the compatibility fingerprint, runner implementation hash, and audit implementation/specification identifiers. No recovery copy is created because `AUDIT-006` authorizes only one checkpoint; the original raw hash must be rechecked immediately before atomic replacement. Resume is permitted from chunk 02 only after migrated checkpoint validation passes through supervisor implementation `whole_lake_boundary_shoreline_audit_windows_supervisor_v2_audit009_migration`, SHA-256 `1abe82c3fb4c5c320bc92b7dc0cc3a66d7a281fab5537aa17aa73929872be0c6`.
- **Rationale:** JSON object-member order has no semantic meaning, but the v8 validator compared the deserialized dictionary order against `STREAMS`. Atomic serialization used sorted keys, so strictly validated chunk 01 reloaded in alphabetical stream-key order and was rejected before chunk 02. Exact key-set validation plus canonical in-memory ordering corrects only that serialization-order defect.
- **Evidence or source:** The exact saved checkpoint and v8 source; read-only preflight and independent validation on 2026-08-23; explicit user approval communicated through the main coordinator on 2026-08-23.
- **Approval provenance:** Explicitly approved by the user through the main coordinator on 2026-08-23 with the exact instruction: “Approve order-independent validation of the four exact stream names, canonical reordering in memory, and migration of only the exact verified checkpoint. No values, calculations, scientific methods, time limits, retry rules, or completed results may change. Resume from chunk 2 after validation passes.”
- **Affected files or components:** `DECISIONS.md`, `tools/run_whole_lake_boundary_shoreline_audit_ee.py`, `tools/supervise_whole_lake_boundary_shoreline_audit.py`, `local_run_state/whole_lake_boundary_shoreline_audit_checkpoint.json`, `docs/WHOLE_LAKE_BOUNDARY_SHORELINE_AUDIT_EXECUTION_GUIDE.md`, and `docs/WHOLE_LAKE_BOUNDARY_SHORELINE_AUDIT_REPORT.md`. This authorizes only the exact validation correction, exact checkpoint migration, and supervised continuation from chunk 02. It does not authorize any value, calculation, scientific method, time limit, retry rule, completed-result, request graph/decomposition, source, geometry, schema, asset, export, task, UI, or deployment change.
- **Superseded decision:** Supersedes only the v8 checkpoint validator's dependence on JSON object-member order and the corresponding prior runner/compatibility/supervisor pins for future resumed execution. It does not supersede the validated content of chunk 01 or any scientific, execution-limit, retry, provenance, geometry, schema, validation, acceptance, or scope requirement of `AUDIT-001` through `AUDIT-008`.

### AUDIT-010 — Four sequential stream requests per logical audit chunk

- **Date:** 2026-08-23
- **Status:** approved
- **Decision:** Retain each approved deterministic 56-row season-treatment logical chunk, its exact 14 dates per stream, four separate streams, scientific processor, QA, formulas, geometry, schemas, summaries, certificates, and final acceptance gates, but evaluate each remaining logical chunk through exactly four sequential 14-row Earth Engine requests in canonical order: Terra day, Terra night, Aqua day, Aqua night. Each request contains only its stream's unchanged 14 rows and remains subject to the hard monotonic 480.000-second wall-clock cutoff, exactly one attempt with no automatic retry, and the hard 165-minute session ceiling. The runner must validate each transient subrequest, combine all four results in memory into the same exact 56 row keys and four summaries, rerun every logical-chunk schema, runtime, provenance, count, formula, and certificate gate, and atomically save the logical chunk only after all four pass. Partial subrequest evidence must never be persisted.

  Advance the runner to `whole_lake_boundary_shoreline_audit_python_api_v10_four_stream_subrequests`, SHA-256 `6cd0bed7ddd4426464da0c7619515a7e341a4585d5ccbc47efcdecb9c9b98774`, compatibility SHA-256 `a1bc3207a18ef4ec66f13fb7f981c8cb0376233be36341c3ac495a19a6e6ad31`, and supervisor `whole_lake_boundary_shoreline_audit_windows_supervisor_v3_audit010_four_stream_subrequests`, SHA-256 `1bee89663acc167df4030e4eb4e4e7ce082daa1887564e434ba370916b638a69`. Authorize one fail-closed atomic in-place compatibility migration only from checkpoint raw SHA-256 `daa9d90c0c6e7ade7480fc2a614aa6a000a9513311a4f778062c679942df6ec6`, prior compatibility `ccdebab8bba4983462a9e3134f2411255fbcb08fa3ded98b2dce906dfe403ca9`, and prior runner `07ec5a72ffe420bd3954b5f4865eb2ba4b4bd86b8338d783f243f5d0e2ef2863`. Migration may add/change only the compatibility fingerprint, runner hash, audit implementation/specification identifiers, and exact request-decomposition metadata. It must prove four unchanged vectors, exact unchanged chunks 01 and 02, unchanged values, canonical hashes and certificates, false acceptance, coordinate-free evidence, and first incomplete chunk 03. No recovery copy, authenticated Earth Engine request, asset, export, task, deployment, or UI is authorized by the migration.

  Independent validation identified that v10 deferred complete summary/runtime admission until all four transient responses existed. The controlling correction is runner `whole_lake_boundary_shoreline_audit_python_api_v11_immediate_stream_summary_gates`, SHA-256 `0e6c33277c2180db756d7ccfd6bf7c0cf9a5895c1ff07384fbd64ac7cf9d218f`, compatibility SHA-256 `5f5bb05a95a22219c550ab124a33b3f00f1795ff25f9e3bf19ff3595ac91b17e`, and supervisor `whole_lake_boundary_shoreline_audit_windows_supervisor_v4_audit010_immediate_stream_gates`, SHA-256 `8614de215b21e1722520f482c22516c04dea13950d58ba194ded252e9cfd7064`. Each stream response must now pass its exact row evidence, coverage, single 45-field summary physical schema/runtime types, provenance, and formulas before the next canonical stream request may be issued. Authorize a second exact compatibility-only migration from raw checkpoint SHA-256 `520e00ea03b2949892e6d847919ba7a7a68fdd5ddcebe6349f59d3d092502199`, v10 compatibility `a1bc3207a18ef4ec66f13fb7f981c8cb0376233be36341c3ac495a19a6e6ad31`, and v10 runner `6cd0bed7ddd4426464da0c7619515a7e341a4585d5ccbc47efcdecb9c9b98774`. It may change only the compatibility fingerprint, runner hash, and audit implementation identifier and must preserve every saved value, vector/chunk hash and certificate, request-decomposition value, false acceptance, and first incomplete chunk 03.
- **Rationale:** The authenticated 56-row chunk-03 request reached the hard 480.000-second cutoff. The approved stream-separated packaging reduces each bounded Earth Engine request to one unchanged 14-row stream while preserving the logical chunk and refusing to persist an incomplete four-stream result.
- **Evidence or source:** Authenticated `AUDIT-009` execution and exact coordinate-free checkpoint postflight on 2026-08-23; the coordinator's immediately preceding narrow four-subrequest proposal; explicit user approval communicated through the main coordinator.
- **Approval provenance:** Explicitly approved by the user through the main coordinator on 2026-08-23 with the exact response “ok, let's continue” to the coordinator's immediately preceding proposal specifying the four sequential stream requests, unchanged science/evidence, atomic logical-chunk save, exact checkpoint migration, 480.000-second cutoff, one attempt/no automatic retry, and 165-minute ceiling.
- **Affected files or components:** `DECISIONS.md`, `tools/run_whole_lake_boundary_shoreline_audit_ee.py`, `tools/supervise_whole_lake_boundary_shoreline_audit.py`, `local_run_state/whole_lake_boundary_shoreline_audit_checkpoint.json`, `docs/WHOLE_LAKE_BOUNDARY_SHORELINE_AUDIT_EXECUTION_GUIDE.md`, and `docs/WHOLE_LAKE_BOUNDARY_SHORELINE_AUDIT_REPORT.md`.
- **Superseded decision:** Supersedes only the single-request packaging of each remaining 56-row logical chunk and the corresponding AUDIT-009 runner, supervisor, and compatibility pins for future resumed execution. It does not supersede or change saved chunks 01–02, any row value, calculation, scientific method, source, geometry, QA rule, schema, formula, certificate, time limit, retry rule, checkpoint acceptance gate, or scope requirement of `AUDIT-001` through `AUDIT-009`.

### AUDIT-011 — Separate nighttime QA gate-attribution diagnostic

- **Date:** 2026-08-23
- **Status:** approved
- **Decision:** Run a separate, diagnostic-only attribution of nighttime coverage loss on the exact verified unrounded WISE WFD v1.9 HUAIH049 2022 polygon at the original `0 m` treatment. Keep Terra night (`MODIS/061/MOD11A1`) and Aqua night (`MODIS/061/MYD11A1`) separate and retain the same four approved 14-day 2024 windows. For every date-stream, measure survival through the declared order: valid provider-range temperature data, mandatory QA, the three detailed quality flags (`data_quality`, `emissivity_error`, and `lst_error`), view-time validity, view-angle validity, and the exact final accepted mask. Report both the declared-order cumulative waterfall and order-independent standalone, leave-one-out, exclusive-failure, and pairwise-failure-overlap evidence; the waterfall is bookkeeping whose increments depend on order, while the overlap-aware evidence is descriptive logical attribution and not a causal claim. Results and logs must be coordinate-free.

  Use diagnostic runner `nighttime_gate_attribution_diagnostic_python_api_v5_boolean_source_identity`, SHA-256 `437c1b03f73cfec2a11eb50b86b236b0fe64574aa2e79c4b012a0d16f377bb9a`, and the versioned Windows supervisor `nighttime_gate_attribution_windows_supervisor_v6_boolean_source_identity_pin`, SHA-256 `3ba66b29592466ac5f3d0f03b662f61839c7e3d80592e0fd8f6ef99ee42cdd1d`. Each exact lifecycle-marked Earth Engine request has a true monotonic `480.000`-second wall-clock cutoff, exactly one attempt with no automatic retry, and complete child-process-tree termination at the cutoff; the whole diagnostic independently has a hard 165-minute session ceiling. The Earth Engine client policy receives the type-exact integer `480` seconds required by the pinned policy validator, while the external monotonic cutoff remains `480.000` seconds; these are the same unchanged duration with distinct API types. The request decomposition is four canonical metadata requests, one runtime-type fixture request, eight window-stream projection-inventory requests, and eight 14-row attribution requests, for 21 sequential Earth Engine requests. Each attribution date requires exactly one source image and no missing/duplicate fallback metric may be emitted. Source/projection identity evidence must serialize as an actual Boolean, not numeric `0/1`; any future local admission failure may emit only allowlisted coordinate-free validation-error codes, never metric values or coordinates. The supervisor independently requires the exact ordered 21 normalized request start/complete positions and one exact terminal coordinate-free acceptance object. The completed shoreline audit runner, checkpoint, evidence, result, and acceptance artifact remain immutable and outside this diagnostic.
- **Rationale:** The completed audit established that nighttime accepted coverage can be much smaller than eligible coverage but was designed to certify final acceptance, not to attribute loss among logically overlapping gates. A separate diagnostic can expose where pixels fail without changing any accepted mask or retrospective audit result. Reporting cumulative and overlap-aware views prevents declared gate order from masquerading as causal importance.
- **Evidence or source:** The completed coordinate-free `AUDIT-010` result and the coordinator's immediately preceding narrow diagnostic proposal; exact gate definitions and bit decoding are reused from the byte-pinned completed audit runner. The authenticated supervised v3 launch attempt with project `ee-jtantaroman` on 2026-08-23 exited code 1 in approximately 4.5 seconds before any `PHASE_REQUEST_START`, emitted only fail-closed `ValueError` class evidence, produced no result, and made no retry. Read-only/local diagnosis proved that v3 passed float `480.000` to the pinned integer-only client-policy validator; runner v4 corrects only that argument type. The second supervised v4 attempt passed request policy and source preflight (coordinate-free source-evidence hash beginning `c6fe12`) and then stopped fail closed with `SupervisorError:LIFECYCLE_EVENT_OR_PHASE_FAILED` before forwarding the first lifecycle record, with no result, retry, acceptance, or checkpoint mutation. Static comparison proved that the byte-pinned audit helper normalizes internal phase identities before emission, while supervisor v4 incorrectly required unnormalized internal labels; supervisor v5 corrected only that protocol schema. The third supervised v5 attempt passed preflight, all four canonical metadata requests, the runtime fixture, all eight projection inventories, and the first attribution request (`8.313` seconds), then failed local response admission with `RuntimeError` and exited code 1, with no terminal result, retry, acceptance, or checkpoint mutation. Static tracing proved the response emitted numeric `0/1` from the pinned `_nbool(...).And(...)` helper for `source_identity_matches_projection_inventory`, while client admission requires actual Boolean `true`; runner v5 applies the pinned `_boolean(...)` serializer only to that evidence field and adds safe code-only admission diagnostics. The corrected runner-v5/supervisor-v6 execution and a separate identical coordinate-free capture execution both exited 0 with all 21 lifecycle phases complete and terminal `pass: true`. Independent strict parsing of temporary capture SHA-256 `f73674d511e672f3d9f8b0347dc9949a30b3cc54bbc5baa6171619f805a67e2b` verified source-evidence SHA-256 `c6fe12b5d031c63bcedcb820b2ded3582195b18497a6efd48ceeed21b3d1363a`, exactly eight window-stream results, 112 single-source accepted records, no validation errors, and one valid terminal object. The raw coordinate-free capture remains outside the repository.
- **Approval provenance:** Explicitly approved by the user through the main coordinator on 2026-08-23 with the exact response “Yes, I approved this, get attributed the diagnostic.” to the coordinator's immediately preceding proposal specifying the diagnostic-only original 0 m WISE boundary, separate Terra/Aqua nighttime streams, the same four 14-day windows, exact gate stages, unchanged science and audit result, coordinate-free evidence, 480.000-second request cutoff, one attempt/no automatic retry, and 165-minute ceiling.
- **Affected files or components:** `DECISIONS.md`, `tools/run_nighttime_gate_attribution_diagnostic_ee.py`, `tools/supervise_nighttime_gate_attribution_diagnostic.py`, `docs/NIGHTTIME_GATE_ATTRIBUTION_DIAGNOSTIC_SPECIFICATION.md`, `docs/NIGHTTIME_GATE_ATTRIBUTION_DIAGNOSTIC_EXECUTION_GUIDE.md`, and `docs/NIGHTTIME_GATE_ATTRIBUTION_DIAGNOSTIC_REPORT.md`. This authorizes the separate diagnostic, its local/static/synthetic and independent validation, and authenticated execution through the validated pinned supervisor after that independent validation passes; no second user approval is required. It does not authorize any QA-filter, formula, scientific-method, source, geometry, completed-audit result, shoreline choice, coverage threshold, application, asset, export, task, UI, or deployment change.
- **Superseded decision:** None. This diagnostic supplements but does not supersede `AUDIT-001` through `AUDIT-010` or any prototype/scientific decision.

### AUDIT-012 — Bounded nighttime QA candidate A/B/C comparison

- **Date:** 2026-08-28
- **Status:** approved; diagnostic run completed 2026-09-02 (`"pass": true`, no candidate selected)
- **Decision:** Run a separate, bounded, coordinate-free comparison of three nighttime QA candidates over the same 112 observations used by `AUDIT-011`—four existing 14-day 2024 windows × Terra night and Aqua night kept separate—and all four existing shoreline treatments. Common validity `V` requires exactly one source image, observed/unmasked QC, provider-range observed/unmasked LST, and provider-range observed/unmasked view time and angle. Candidate A is `V & M=0`, with D/E/T reported but not gating. Candidate B is `V & M∈{0,1} & D=0 & E=0 & T=0`, with M0/M1 tiers retained separately. Candidate C is `V & M∈{0,1} & D=0 & E≤1 & T≤1`, preserving the strict B subset and splitting moderate `C\B` additions by M0/M1. D2/3 remain excluded from B/C and D1 is diagnostic only. A is non-nested with B/C and must not be described as strictest or loosest. All three are evaluated simultaneously without selection or adoption.

  Retain exact conditional-on-V QC-byte pixel-count and area histograms for all bytes `0–255`, the exact decoded M/D/E/T joint evidence, M0,D>0 and M1 subclasses, QC/LST mask-and-fill/range cross-tabs, candidate/tier daily coverage and missing/zero dates, seasonal summaries, the A∩B/A-only/B-only/C-minus-B temperature and ancillary distributions and contrasts, and shoreline sensitivity. Deterministically select the earliest M0,D>0 date per stream-season where available and verify a bounded sample of exact original NASA Collection 6.1 HDF values and masks against Earth Engine; retain no source coordinates, pixel indices, raw values, or raster bytes. Any unsupported source-access mechanic, nonunique source/granule, mismatch, or incomplete sample fails closed without substitution.

  Every request retains a hard monotonic `480.000`-second wall-clock cutoff, exactly one attempt with no automatic retry, and complete process-tree termination; the independent whole-session ceiling remains 165 minutes. This comparison creates no application, asset, export, task, UI, deployment, checkpoint, or changed audit result and does not resolve `QA-002` or approve a final QA rule.

  Independent validation of v1 found that its source verifier conflated Earth Engine's actual date-only image identity with a NASA producer-granule identifier and did not yet prove the complete zero-redirect/cleanup/terminal-evidence contract. The controlling correction retains exact Type-7 pixel summaries through explicit sorted-list `h=(n-1)p` interpolation, samples only the deterministic earliest anomaly date per stream-season, separates and cross-validates EE/projection-inventory/NASA identities, disables redirects, accounts for every CMR/HDF transaction, closes every HDF handle and verifies temporary-file deletion in all paths, and independently revalidates all source evidence in the supervisor. Final validation also corrected the source tile from erroneous `h18v04` to Lake Balaton's MODIS sinusoidal `h19v04`: the runner derives the tile from transient global pixel indices generated in the pinned canonical projection, requires every sample to resolve to `(h,v)=(19,4)`, and converts the HDF column as `global_x - 19*1200` with the exact `0..1199` bound before constructing the NASA producer prefix. Independent implementation review on 2026-08-31 (`docs/NIGHTTIME_QA_CANDIDATE_COMPARISON_INDEPENDENT_REVIEW.md`) drove three fail-closed corrections, none changing a candidate definition, evidence semantics, request count, limit, or decision. v4 fixed a blocking defect: the per-group value distribution indexed a possibly-empty Earth Engine list inside `ee.Algorithms.If`, which evaluates both branches and raises server-side for any zero-pixel group (the dominant nighttime case); it now indexes an always-non-empty list and returns explicit null `mean`/`min`/`max`/percentile statistics for empty groups with real zero counts, leaving the exact Type-7 `h=(n-1)p` estimator unchanged. v5 additionally (a) selects the in-graph sampling date with the identical `self._count` pixel-centre count used by the Python-side `select_source_verifications()`, so the two "earliest M0,D>0 date" derivations are consistent by construction, and (b) replaces the strict single-CMR-granule requirement with deterministic retention of the latest 13-digit production timestamp when NASA lists an original plus reprocessed duplicates (`hits` must equal the returned item count, at most 20; identical timestamps fail closed), recording `cmr_granules_considered` and `selected_production_timestamp` in the source evidence. v6 fixed a blocking defect surfaced by the first authenticated launch on 2026-08-31: `ComparisonRunner.graph` called `self._frequency` (256-bin QC histogram and 6-bin cross-tab), which the AUDIT-011 delegation list omits, so comparison request 14 of 45 raised `AttributeError` during graph construction (~0.1 s, before any server round-trip); v6 binds `_frequency` on `ComparisonRunner` from the pinned AUDIT-010 `AuditRunner` and adds a static offline check that every graph-path primitive resolves. Requests 1–13 (four canonical metadata, one runtime fixture, eight projection inventories) completed, confirming Earth Engine authentication, the `ee-jtantaroman` project, and the pyhdf/token preflight. v7–v8 bundle further fail-closed corrections, none changing a candidate definition, an evidence field, a per-request limit, the 480 s cutoff, the 165-minute session ceiling, or the decision. The Earth Engine request count rose from 45 to 117, still a fixed bounded sequential plan. (a) Concurrency: a single 14-date comparison request fans out into ~1,500 concurrent Earth Engine aggregations, and the fourth-to-fifth run back-to-back trips a 429 `Too many concurrent aggregations` (accumulated load — the same request passes in isolation). A drain pause only reduces the odds (25 s cleared winter 8/8 but not summer; 45 s cleared summer 8/8 but the full run still hit an occasional 429). **v8 splits every `(window, stream, treatment)` comparison into three fixed 5-date batches** (`DATE_BATCHES = ((0..4),(5..9),(10..13))`), each far under the concurrency ceiling regardless of load, adds one lightweight `earliest_anomaly_position` probe per `(window, stream)` (8 total, same pixel-centre predicate as `select_source_verifications()`), and reassembles the 14 per-date records in Python before `validate_result`. A short `COMPARISON_REQUEST_PAUSE_SECONDS = 12.0` precedes each batch. (b) `metric()` computed `_sum_area(mask)` twice per candidate; now once, with `coverage_fraction == area_m2 / eligible` exactly (~15 % fewer per-request aggregations). (c) `_validate_distribution` compared the Type-7 chain and mean bound with exact `≤`; sub-ULP interpolation rounding puts a percentile ~1e-15 outside `[min, max]` for tightly clustered groups → `AdmissionFailure`. Fixed with a `1e-9`-relative tolerance that still rejects a genuine ordering fault. (d) `one_date` extracted anomaly samples with random `Image.sample(numPixels=16, seed=…)`, which returns nothing for a 1–2-pixel mask → `ANOMALY_WITHOUT_BOUNDED_SOURCE_SAMPLE`. v8 uses `Reducer.toList()` (every anomaly pixel, native-grid scan order, first `SAMPLE_LIMIT`, no seed). (e) `ee.Image.pixelCoordinates` returns pixel-centre coordinates (index + 0.5); the MODIS-tile derivation requires integer indices, so every real sample failed `SOURCE_SAMPLE_INDEX_FAILED`. v8 floors `global_x`/`global_y`. (f) `validate_result` built the transient-sample dict without a `window_id` key that `execute()` reads to key `transient_by_key` → `KeyError` on the first treatment with a real sample; v8 adds the key. (g) The runner's catch-all now prints the coordinate-free-screened Earth Engine / transport exception message. Faults (c)–(f) were latent from v1, each masked by the one before it, and surfaced only when authenticated runs first reached the corresponding code; an authenticated pre-network test over all eight `(window, stream)` at 0 m now exercises assembly, `validate_result`, transient collection, `select_source_verifications` (5 selected / 3 not-applicable), `verify_original_source` up to the CMR request, and `temperature_selectivity` with zero errors. v9 fixes a further blocking fault surfaced by the fourth authenticated launch on 2026-08-31, which completed all 117 Earth Engine requests and then failed the first NASA CMR search with `CMR_GRANULE_IDENTITY_FAILED`: the CMR parser assumed a UMM-G shape that MOD11A1/MYD11A1 Collection 6.1 does not have — `GranuleUR` is the producer granule id with no `.hdf` suffix, the HTTPS download URL carries an extra `<granule-id>/` directory before the file name, and UMM-G holds no per-file checksum for this product. v9 reads the granule identity (13-digit production timestamp = the whole `GranuleUR` tail after the `<short_name>.A<year><doy>.h19v04.061.` prefix) and the single allowlisted HTTPS `.hdf` URL from UMM-G as they are, and issues one additional bounded request per selected granule to CMR's legacy `echo10` metadata (`GET https://cmr.earthdata.nasa.gov/search/concepts/<concept-id>.echo10`, ≤ 5 MB, redirects disabled, exact URL identity, one transaction) to read the archived `.hdf` file's declared SHA-256 (or MD5) checksum and its exact `SizeInBytes`. `verify_hdf_bytes` now enforces both the declared checksum and the declared byte size against the downloaded bytes (new `HDF_DECLARED_SIZE_MISMATCH`). The source evidence gains `declared_size_bytes`, `declared_size_verified`, and a `meta_transaction` gate; `network_transaction_count` per verified granule is 3 (cmr → echo10 → HDF); the supervisor source lifecycle enforces the exact per-identity order cmr → meta → hdf and the NASA request ceiling rises from 16 to 24 (≤ 8 anomalies × 3). v10 then responds to a further blocking condition on a 2026-09-01 launch, which completed setup and several 5-date comparison batches and then failed one batch with `EarthEngineException: Earth Engine memory capacity exceeded` — an Earth Engine per-request compute-size limit, distinct from the 429 concurrency limit, that also varies with service load (v8's launch 4 had completed the same phase). v10 halves `COMPARISON_DATE_BATCH_SIZE` from 5 to 2 (`DATE_BATCHES` becomes seven 2-date batches per treatment → 224 comparison requests, 245 Earth Engine requests total) and lowers `COMPARISON_REQUEST_PAUSE_SECONDS` from 12.0 to 8.0. The graph, masks, candidate definitions, histograms, distributions, evidence, and the Python reassembly are unchanged; no per-request limit, the 480 s cutoff, the 165-minute session ceiling, or the decision changes. v11 then responds to launch 5 (2026-09-01), which completed all 245 Earth Engine requests, the CMR `umm_json` search, and the `echo10` checksum fetch, and failed the authenticated HDF download with `NASA_REDIRECT_FORBIDDEN`: LP DAAC Earthdata Cloud answers a bearer-authenticated protected-granule GET with a 302 to a short-lived pre-signed AWS S3 URL, and there is no zero-redirect way to fetch a protected granule. **With explicit operator approval on 2026-09-01, v11 relaxes the specification's original "zero redirects / exact final URL" rule for the HDF download only:** the download follows the LP DAAC redirect, re-requesting the signed URL without the `Authorization` header (the signed URL authenticates itself), still enforcing the `echo10` SHA-256 and byte size on the returned bytes, and recording the hop in `hdf_transaction` (`redirect_count` 1–2, plus the `final_host`/`final_path`; the query string, a credential, is stored only as a hash). v11's follow-hop allowlist was AWS-S3-only; launch 6 (2026-09-01) showed the real LP DAAC egress redirect goes to a CloudFront distribution (`d1nklfio7vscoe.cloudfront.net` — the LP DAAC "Egress" CDN, whose landing page carries the same Earthdata OAuth client id as `data.lpdaac.earthdatacloud.nasa.gov`), diagnosed immediately from the v11 `NIGHTTIME_QA_HDF_REDIRECT_REJECTED` line. **v12 widens the download-redirect allowlist to `*.cloudfront.net`, AWS S3 hosts, and `data.lpdaac.earthdatacloud.nasa.gov` itself, allows up to two validated hops, and requires the granule `.hdf` name to survive as a full path segment; suffix-attack hosts (`…cloudfront.net.evil.net`) are rejected.** The bearer token is only ever sent to `data.lpdaac.earthdatacloud.nasa.gov`; a redirect to any other host fails closed. `_NoRedirect` still forbids automatic redirect following; the CMR and `echo10` requests are unchanged (zero redirects). `network_transaction_count` per verified granule stays 3; no per-request limit, cutoff, session ceiling, candidate definition, or the decision changes. v13 then fixes the last blocking fault surfaced by launch 7 (2026-09-01, at v12), which completed all 245 Earth Engine requests, the CMR search, the `echo10` fetch, the authenticated HDF download **and its `echo10` checksum + byte-size verification** — the first end-to-end authenticated download — then failed in `verify_hdf_bytes` opening the file with `HDF4Error: select: non-existent dataset`: the code selected HDF-EOS2 SDS `Night_view_angle`, but the MOD11A1/MYD11A1 dataset name is truncated to `Night_view_angl` (no trailing "e"), confirmed against NASA's Earthdata product catalog. v13 uses `Night_view_angl` in the `verify_hdf_bytes` SDS mapping, the `validate_source_evidence_item` `expected_sds` list, and `synthetic_source_evidence`; the Earth-Engine-side band name stays `Night_view_angle` (Earth Engine's own cleaned-up name). No candidate definition, metric, per-request limit, cutoff, session ceiling, or the decision changes. v14 then fixes the last blocking fault surfaced by launch 8 (2026-09-02, at v13), which completed all 245 Earth Engine requests, the CMR search, the `echo10` fetch, the authenticated CloudFront-hop HDF download, the `echo10` SHA-256 + byte-size verification, the HDF file open, and all five SDS shape checks, then failed the final per-pixel cross-check with `HDF_EE_SAMPLE_MISMATCH`. A bounded coordinate-free diagnostic against the real `MOD11A1.A2024019.h19v04` granule established that the three uint8 SDS (`QC_Night`, `Night_view_time`, `Night_view_angl`) matched Earth Engine exactly at the derived tile index — confirming the pixel index and grid orientation are correct — while pyhdf's scalar-index read `selected[row, col]` returns a constant `1` for the two uint16 SDS (`LST_Night_1km`, `Clear_night_cov`); a full-array `selected.get()` (and slice reads) return the true values, which then match Earth Engine exactly for all five bands across all eight samples. v14 reads each 1200×1200 SDS once with `.get()` and indexes the NumPy array (with a `(1200, 1200)` shape assertion on the array). No candidate definition, metric, mask, per-request limit, cutoff, session ceiling, request count, evidence schema, or the decision changes. Controlling pins are runner `nighttime_qa_candidate_comparison_python_api_v14_h19v04_date_batched_comparison`, SHA-256 `0682897404d20498996e7a73742c604ad350ad409cbb6c40b3071111a5d16760`, and supervisor `nighttime_qa_candidate_comparison_windows_supervisor_v14_h19v04_date_batched_comparison`, SHA-256 `27c8d71b3d54d2faa4af39be609748263033b053c92d38641c3c2a91d5ce9d55`. Offline `py_compile` and both `--self-test` suites pass at the v14 pins; authenticated tests return zero 429s and zero "memory capacity exceeded" (including 56 consecutive 2-date comparison batches on 2026-09-01 across all four windows, both streams, and five treatments), every batch piece and reassembled `validate_result` returns zero errors, the four anomaly stream-seasons produce samples that pass MODIS-tile derivation and NASA-identity construction, the live CMR `umm_json` + `echo10` parse path is confirmed against the five real anomaly granules (granule id, checksum, exact byte size), the LP DAAC download-redirect-reject path is confirmed against real LP DAAC, launch 7 confirmed the authenticated CloudFront-hop HDF download plus checksum and byte-size verification on real data, and the launch-8 diagnostic confirmed every step of `verify_hdf_bytes` — including the per-pixel value/mask cross-check for all five bands — passes on the real granule with the `.get()` read. **The full v14 supervised run completed 2026-09-02** (`$HOME/audit012_run_2026-09-02_v14.log`, exit 0): `NIGHTTIME_QA_CANDIDATE_COMPARISON_COMPLETE` with `"pass": true`, `validation_errors: []`, 245/245 Earth Engine phase markers, 15/15 NASA source requests (5 selected `(window, stream)` × cmr+echo10+hdf), 32 results, 448 treatment records, zero `STOP`/`FAILED`/`REJECTED`/`MISMATCH` markers; the supervisor independently re-validated the entire terminal payload and exited 0. All 5 selected granules returned `verified_exact_original_hdf_samples` with checksum + byte size + all EE values and masks exact (1–8 samples each); `spring/terra`, `summer/terra`, `summer/aqua` had no M0,D>0 pixel (`not_applicable`). Result (feeds `QA-002`, selects nothing): 98.7 % of all valid-water nighttime pixels are QC byte 65 (`M1/D0/E0/T1` — LST error ≤ 2 K, not ≤ 1 K); candidate A keeps ~0.5 % of common validity, candidate B keeps **0 %** (nothing at night has `T=0`), candidate C keeps ~99 %. Shoreline erosion lowers C's coverage fraction by ~0.9–1.5 pp at 460–925 m; A unaffected; B zero. Full numbers in `docs/NIGHTTIME_QA_CANDIDATE_COMPARISON_REPORT.md`. No QA candidate is selected; `AUDIT-012` is complete.
- **Rationale:** `AUDIT-011` showed that the strict engineering mask produced zero final nighttime coverage in these windows and attributed the surviving area primarily to mandatory/data-quality interactions. A simultaneous, overlap-aware candidate comparison is needed to explain the practical consequences of plausible interpretations without silently choosing a replacement rule.
- **Evidence or source:** Completed `AUDIT-011` coordinate-free evidence; official MOD11A1/MYD11A1 Collection 6.1 QC-byte definitions; the coordinator's immediately preceding bounded-comparison proposal; explicit user approval communicated through the main coordinator on 2026-08-28.
- **Approval provenance:** Explicitly approved by the user through the main coordinator with the exact response “ok, let's start with the comparison, but could u please explain more about A/B/C QA candidates? I couldn't undrstand properly the diferrence between them and their definition” to the coordinator's immediately preceding proposal specifying the same 112 nighttime observations, four shoreline treatments, simultaneous nonselective A/B/C comparison, exact joint-QC and selectivity evidence, deterministic original-NASA-source verification, coordinate-free outputs, unchanged application/audit, `480.000` seconds per request, one attempt/no automatic retry, and 165-minute session ceiling.
- **Affected files or components:** `DECISIONS.md`, `requirements-geometry-audit.txt`, `docs/NIGHTTIME_QA_CANDIDATE_COMPARISON_SPECIFICATION.md`, `docs/NIGHTTIME_QA_CANDIDATE_COMPARISON_EXECUTION_GUIDE.md`, `docs/NIGHTTIME_QA_CANDIDATE_COMPARISON_REPORT.md`, `docs/NIGHTTIME_QA_CANDIDATE_COMPARISON_INDEPENDENT_REVIEW.md`, `tools/run_nighttime_qa_candidate_comparison_ee.py`, and `tools/supervise_nighttime_qa_candidate_comparison.py`. No existing completed audit or `AUDIT-011` implementation/result file is modified.
- **Superseded decision:** None. This diagnostic supplements but does not supersede `AUDIT-001` through `AUDIT-011`, the strict prototype engineering rule, or any unresolved QA decision.

### AUDIT-013 — Historical observation-availability probe

- **Date:** 2026-09-02
- **Status:** **complete (2026-09).** User confirmed the specification ("looks right execute and give me a brief result"). Runner `tools/run_historical_observation_availability_ee.py` SHA-256 `514242b69646c0e981cba59a00821de3705fb0388ea08650b4fb14665de45e75`; supervisor `tools/supervise_historical_observation_availability.py`. Both `--self-test` suites pass. The supervised run finished with exit 0, `AUDIT013_SUPERVISOR_COMPLETE` / `terminal_revalidated: true`, 192 cells, `cells_sha256 97cdf82e…`. **Result:** at a ± 5-day window every stream × month has 20/20 historical years covered (one cell 19/20) with 110–195 total daily observations behind every percentile — the historical sample is not thin; clouds thin the per-day pixel coverage, not the number of days. Full result in `docs/HISTORICAL_OBSERVATION_AVAILABILITY_REPORT.md`.
- **Decision:** Authorize a bounded, read-only, coordinate-free counting diagnostic that measures, from the real MODIS archive over 2003–2022, how many usable historical observations sit behind a percentile — for each of the four streams, the 15th of each month, and window half-widths of 5, 7, 10, and 15 days. It reports observation *counts* only (years with any accepted observation, total accepted daily observations, per-year counts, median accepted pixel count on positive days) using the approved lake-wide 0 m geometry and the approved `QA-002` candidate-C acceptance rule (reusing the byte-pinned `AUDIT-012` mask code). It computes no anomaly, climatology, percentile, or classification, selects no `METH-*` value, changes no completed audit or approved decision, and creates no Earth Engine asset, export, task, UI, or deployment. It never reads a temperature or a coordinate. Same guardrails as `AUDIT-011` / `AUDIT-012`: coordinate-free runner + Windows Job Object supervisor, `480.000`-second per-request cutoff, one attempt / no automatic retry, 165-minute session ceiling, byte-pinned runner. No NASA CMR / Earthdata / HDF access.
- **Rationale:** `METH-001` (seasonal window width) and `METH-003` (minimum-sample thresholds) cannot be fixed responsibly without knowing the real historical sample size, which `AUDIT-011` / `AUDIT-012` showed is far below the ~220 nominal at night. Measuring it directly is cheaper and more honest than guessing.
- **Evidence or source:** `docs/HISTORICAL_OBSERVATION_AVAILABILITY_SPECIFICATION.md`; `docs/METH_001_006_PROPOSAL.md` §2.2, §4; `AUDIT-011` and `AUDIT-012` evidence; approved `QA-002` and `SPACE-001`.
- **Approval provenance:** User asked to proceed with the check (2026-09, "lets go with the small check on how much history we actually have"); the specification is presented for confirmation before code is written.
- **Affected files or components:** `DECISIONS.md`, `docs/HISTORICAL_OBSERVATION_AVAILABILITY_SPECIFICATION.md`, a new coordinate-free runner and Windows supervisor under `tools/`, and (on completion) an availability report. No existing completed audit, prototype, or `AUDIT-011` / `AUDIT-012` file is modified.
- **Superseded decision:** None. Supplements the `METH-*` decision process; supersedes nothing.

### PHASE-3 — Anomaly engine implementation

- **Date:** 2026-09-02
- **Status:** approved; **Phase 3 output is built** (2026-09-03). The 2003–2022 climatology baseline (`docs/PHASE_3_CLIMATOLOGY_REPORT.md`, `baseline_rows_sha256 1ff9ca8d…`), the daily anomaly records 2023 → 2026-09 (`records_sha256 dfbd4f13…`, 3,608 records), and the monthly summaries (`rows_sha256 8e69b45d…`, 176 rows) all exist. Results: `docs/PHASE_3_MONITORING_RESULTS_REPORT.md`. `ARCH-001` and Phase 4 follow. User: "let's build phase 3" / "yes, let's run it" / "ok, run it".
- **Decision:** Implement the Phase 3 anomaly engine per `docs/PHASE_3_ANOMALY_ENGINE_SPECIFICATION.md`, applying the approved `SPACE-001` / `QA-002` / `METH-001`…`006` decisions with no new scientific choice. Runner `tools/run_anomaly_engine_ee.py` SHA-256 `f649df317cc0694b8051d608f6040c7b094c706d6f4b5916eb748c1b92a8510b` (`anomaly_engine_python_api_v1_lake_wide_candidate_c`); supervisor `tools/supervise_anomaly_engine.py` (`anomaly_engine_windows_supervisor_v1`). Produces four coordinate-free local artefacts under `local_run_state/phase3/` (git-ignored): the historical daily lake values (2003–2022), the ± 5-day day-of-year climatology baseline (`METH-001`; median + mean + sorted list + `median_minus_mean_c`, `METH-002`), the daily anomaly records (`METH-003`/`004` percentile + label + `QA-002` confidence tier), and the stream-specific monthly summaries (`METH-005`). It creates no Earth Engine asset, export, task, or deployment — that is `ARCH-001`. It caches a **hash-pinned local copy** of the `AUDIT-003` verified geometry so a production run does not depend on the sometimes-unavailable EEA provenance servers.
- **Geometry verification, incl. the CDR-endpoint fallback (user-approved 2026-09):** the engine first attempts the full `AUDIT-003` provenance chain via the byte-pinned `verify_wise_huaih049.py`. If — and only if — that fails because the EEA CDR-metadata endpoint (`sdi.eea.europa.eu/datashare/…/CDR_metadata.csv`) is unreachable (it has been 503 since ~2026-09-02), the engine re-fetches the polygon from the working WISE geometry service and re-checks it with the byte-pinned `analyze_wise_huaih049_source_representations.py` parser (SHA-256 `e3c0f1a0…`) against the `AUDIT-003` expected **raw** SHA-256 `5d5f9c8e…` and **canonical** SHA-256 `394a9296…`, 427,942 canonical bytes, 11,012 tuples, valid/simple Polygon — skipping only the CDR-metadata and GML provenance cross-checks, which *established* rather than *confirm* the pinned identity. Any mismatch fails closed. First run 2026-09: the fallback fired and the refetched polygon matched the `AUDIT-003` raw and canonical hashes **byte-for-byte**; the verified copy is now cached and later runs use it with no EEA access. Approved by the user: "yes, let's run it" to the coordinator's proposal of exactly this fingerprint fallback.
- **Rationale:** Phase 3 is the core deliverable (`SCOPE-003`); the decisions it needs are all approved. Same fail-closed / coordinate-free / byte-pinned / supervised discipline as `AUDIT-011`…`AUDIT-013`.
- **Evidence or source:** `docs/PHASE_3_ANOMALY_ENGINE_SPECIFICATION.md`; approved `SPACE-001`, `QA-002`, `METH-001`…`006`, `SCOPE-003`; `AUDIT-013`; `AUDIT-003` (geometry identity).
- **Approval provenance:** User approved via the main coordinator (2026-09): "first, I would like to apply two, median and average … let's build phase 3"; and the geometry-only CDR-endpoint fallback: "yes, let's run it".
- **Affected files or components:** `tools/run_anomaly_engine_ee.py`, `tools/supervise_anomaly_engine.py`, `local_run_state/phase3/` (already git-ignored via the existing `/local_run_state/` rule), `docs/PHASE_3_ANOMALY_ENGINE_SPECIFICATION.md`. Byte-pins (read-only, unchanged) `tools/run_whole_lake_boundary_shoreline_audit_ee.py` and `tools/analyze_wise_huaih049_source_representations.py`. No completed audit, prototype, or `AUDIT-011`…`013` file is modified.
- **Superseded decision:** None.

## Proposed decisions requiring explicit user approval

The following entries are unresolved. Their presence here does not authorize implementation.

### METH-001 — Historical seasonal window

- **Date:** 2026-08-13
- **Status:** **approved (2026-09)** on the basis of `docs/METH_001_006_PROPOSAL.md` and the completed `AUDIT-013` sample-availability probe. Approval provenance: the user, after reviewing the recommendations and the `AUDIT-013` result and asking clarifying questions, confirmed "ok, everything looks good" and "ok, I understand let's proceed with the phases".
- **Decision:** Decide whether to retain the inherited candidate of ±5 calendar days or use another window or smoother.
- **Proposal (2026-09-02) — reinforced by `AUDIT-013` (2026-09):** day-of-year window of **± 5 days** across 2003–2022, per stream, on the whole-lake unit, for **all four streams**. `AUDIT-013` measured the real historical sample: at ± 5 days every stream × month has 20/20 historical years covered (one cell 19/20) and 110–195 daily observations behind every percentile, so the nighttime-widening contingency is **not triggered** and is dropped. Smoothed seasonal climatology is a future improvement, not the MVP (the raw ± 5-day window has ample data). 29 February merges with 28 February. Awaiting the final `METH-001`…`006` approval.
- **Rationale:** A seasonal matching rule is required to construct historical comparisons.
- **Evidence or source:** `PROJECT_CONTEXT.md`, open decision 1.
- **Approval provenance:** Not approved; carried forward as an open proposal from the attached conversation.
- **Affected files or components:** Climatology, anomaly, percentile calculations.
- **Superseded decision:** None.

### METH-002 — Historical reference statistic

- **Date:** 2026-08-13
- **Status:** **approved (2026-09), revised at the user's request to report both statistics.** Approval provenance: the user reviewed the recommendations and the `AUDIT-013` result, then asked "why should we choose one and not the other? ... let's apply two, median and average" — so both are computed and reported.
- **Decision:** Select the historical reference statistic: mean, median, smoothed seasonal climatology, or another estimator.
- **Approved (2026-09):** compute and store **both** the historical **median** and the historical **mean** of the matched ± 5-day, 2003–2022, same-stream sample, plus the standard deviation. What the application shows:
  - **The thermal-status label is single, and comes from the percentile — not from the mean or the median.** `METH-004` classifies the observation by its rank in the *full historical distribution* (`historical_percentile`). There is exactly one label per observation regardless of this decision. "Two statistics" does **not** mean "two labels".
  - **The headline °C anomaly is `observation − historical median`** (`anomaly_vs_median_c`). It is the prominent number in the daily view. Median because: LST daily values carry occasional bad points (thin cloud, mixed pixels) that pull a mean; the `PHASE-3` climatology showed the nighttime distributions are materially skewed (long cold tail — median sits ~0.6 °C, up to 2.6 °C, above the mean on ~60 % of the year), so mean-based anomalies would read systematically "too warm" at night; and "half of past observations were warmer, half cooler" is a statement a user can interpret.
  - **`anomaly_vs_mean_c` and `median_minus_mean_c` are secondary** — shown in a details panel / the downloadable data and the technical report, not the headline. `median_minus_mean_c` doubles as a "this date's history is skewed" flag when large, and the mean + SD support any later standardized-index work (`METH-006`, optional).
  - A smoothed seasonal climatology remains a documented future improvement.
- **Rationale:** An explicit estimator is required before anomalies can be implemented.
- **Evidence or source:** `PROJECT_CONTEXT.md`, open decision 2.
- **Approval provenance:** Not approved.
- **Affected files or components:** Climatology and anomaly calculations, metadata.
- **Superseded decision:** None.

### METH-003 — Percentile estimator and sample requirement

- **Date:** 2026-08-13
- **Status:** **approved (2026-09)** on the basis of `docs/METH_001_006_PROPOSAL.md` and the completed `AUDIT-013` sample-availability probe. Approval provenance: the user, after reviewing the recommendations and the `AUDIT-013` result and asking clarifying questions, confirmed "ok, everything looks good" and "ok, I understand let's proceed with the phases".
- **Decision:** Select the percentile estimator and minimum historical sample count per spatial unit and observation stream.
- **Proposal (2026-09-02) — confirmed by `AUDIT-013` (2026-09):** estimator = **Type 7** (Hyndman–Fan definition 7, linear interpolation — already used in `AUDIT-010`/`AUDIT-012`). Minimum sample `n` (valid historical observations in the matched ± 5-day window, same stream, whole-lake unit): `n ≥ 20` → full result with percentile and label; `10 ≤ n < 20` → percentile + label with a "limited historical sample" flag; `n < 10` → °C anomaly only, "insufficient history for a percentile"; `n = 0` → observation shown with value and coverage only. The monitoring observation is never added to its own historical sample. `AUDIT-013` shows the real pools are 110–195, so these thresholds are retained as a guardrail (for an unusual gap, or a future basin unit) but essentially never bind on the lake-wide product — every percentile is computed at `n ≥ 20`. The operative reliability signal is the per-day spatial coverage, which the approved `QA-002` confidence tier already carries.
- **Rationale:** Percentiles and their reliability depend on estimator and sample sufficiency.
- **Evidence or source:** `PROJECT_CONTEXT.md`, open decision 3.
- **Approval provenance:** Not approved.
- **Affected files or components:** Percentile calculations, QA, classification, reporting.
- **Superseded decision:** None.

### METH-004 — Classification thresholds and labels

- **Date:** 2026-08-13
- **Status:** **approved (2026-09)** on the basis of `docs/METH_001_006_PROPOSAL.md` and the completed `AUDIT-013` sample-availability probe. Approval provenance: the user, after reviewing the recommendations and the `AUDIT-013` result and asking clarifying questions, confirmed "ok, everything looks good" and "ok, I understand let's proceed with the phases".
- **Decision:** Select classification thresholds and exact labels. The inherited 90th/95th-percentile scheme remains only a proposal.
- **Proposal (2026-09-02, not approved):** percentile-based, applied only when `METH-003` reports a percentile — `< 10th` = `below normal`; `10th–90th` = `within normal range`; `90th–95th` = `warm`; `95th–99th` = `unusually warm`; `≥ 99th` = `extreme warm observation`. When `n < 10` the label reads `historical context unavailable` (°C anomaly only). No "heatwave"/event language (`TERM-001`).
- **Rationale:** Thresholds and labels materially determine reported thermal status.
- **Evidence or source:** `PROJECT_CONTEXT.md`, open decision 4.
- **Approval provenance:** Not approved.
- **Affected files or components:** Classification logic, interface, summaries, documentation.
- **Superseded decision:** None.

### SPACE-001 — Spatial units and authoritative geometries

- **Date:** 2026-08-13 (lake-wide geometry approved 2026-09, after the supervisor consultation)
- **Status:** approved for the lake-wide boundary; basin subdivision and littoral/pelagic zones deferred to `SCOPE-003`.
- **Decision:** The authoritative spatial unit for the six-week minimum viable product is the **whole lake as a single polygon**, using the verified unrounded WISE WFD v1.9 `SurfaceWaterBody_polygon` for `thematicIdIdentifier = HUAIH049`, reporting year 2022 — the exact geometry already verified and used in `AUDIT-001` … `AUDIT-012` (raw SHA-256 `5d5f9c8edfced710fcc8657a6aceb398f14cd815d1c5e41a7df9f00601f312d8`, canonical SHA-256 `394a929617bb975d2f3e3aa627abe26dbcc98f42fa48fe038757b3e734c35020`, one part, no holes, 11,012 tuples). Source coordinates remain transient and are never persisted in the repository, an Earth Engine asset, or an export, per `AUDIT-003`/`AUDIT-006`. The four named basins (Keszthely, Szigliget, Szemes, Siófok) and any littoral/pelagic split are **not** in the minimum scope: no authoritative Hungarian basin geometry is available, and the academic supervisor could not point to one; adding basins would require either an external geometry source or an approved derivation (e.g. splitting the polygon at the recognised narrows), which is treated as an optional extension under `SCOPE-003`.
- **Rationale:** Spatial comparisons and coverage metrics require an approved boundary. The whole-lake WISE polygon is the only authoritative geometry available, is already fully verified, and is sufficient for a lake-wide anomaly product. Basin-level output was always conditional on a usable basin geometry, which does not exist.
- **Evidence or source:** `PROJECT_CONTEXT.md` open decision 5; `AUDIT-001` … `AUDIT-012` and their reports; `docs/WHOLE_LAKE_BOUNDARY_SHORELINE_AUDIT_REPORT.md`; `docs/WHOLE_LAKE_GEOMETRY_REPAIR_SENSITIVITY_REPORT.md`; the 2026-09 supervisor consultation, at which the supervisor gave no specific methodological recommendation and confirmed no authoritative Hungarian boundary was available.
- **Shoreline treatment (approved 2026-09):** the analysis uses the **full polygon with no inward erosion (0 m)** for the six-week MVP. `AUDIT-001` … `AUDIT-012` tested 0 m, 463.31 m, 500 m, and 926.63 m; `AUDIT-012` showed inward erosion lowers the accepted nighttime coverage fraction by ~0.9 pp (463 m) to ~1.5 pp (927 m). 0 m is chosen because (a) nighttime coverage is already very low and cannot afford further loss, (b) anomaly detection is relative — the same near-shore mixed pixels are in both the 2003–2022 baseline and the monitoring observation, so land-mixing bias largely cancels in the anomaly, and (c) Lake Balaton is narrow, so aggressive erosion removes whole cross-sections. The MODIS ~1 km mixed-pixel limitation is stated in the technical report; the choice is revisited if `VAL-001` validation shows a near-shore artifact (especially a summer daytime warm bias).
- **Approval provenance:** Approved by the user through the main coordinator after the 2026-09 supervisor consultation: "there is no hungarian boundary that we can point, so from now let's use the whole lake that we have" (lake-wide geometry) and the follow-up confirmation of the coordinator's recommendations, including "use the full lake outline (0 m)".
- **Affected files or components:** Geometry handling, masks, coverage summaries, maps, project scope, Phase 3–4 implementation.
- **Superseded decision:** None. Does not change any completed audit.

### QA-002 — Valid-water coverage and QA thresholds

- **Date:** 2026-08-13 (nighttime acceptance rule approved 2026-09, after the supervisor consultation)
- **Status:** **approved** (2026-09, after the supervisor consultation). The per-pixel acceptance rule (day and night), the daily confidence-tier principle, and the monthly minimum are all decided; only the exact numeric confidence-tier cut-off is deferred to Phase 3–4 calibration. Full proposal: `docs/QA_002_PROPOSAL.md`.
- **Decision:** Select the per-pixel MODIS QA acceptance rule (day and night), the minimum valid-water coverage for a confidently reported daily observation, and the minimum valid-observation count/coverage for a monthly summary.
- **APPROVED (2026-09) — nighttime acceptance rule = candidate C:**
  ```
  accepted_night = E && Q && L && Vt && Va
               && mandatory_qa ∈ {0, 1}
               && data_quality == 0
               && emissivity_error <= 1      # average emissivity error ≤ 0.02
               && lst_error <= 1             # average LST error ≤ 2 K
  ```
  `AUDIT-011` showed the prototype strict rule (`QC byte == 0`) yields 0 % final nighttime coverage on all 8 window-streams / all 56 dates; `AUDIT-012` showed candidate A ≈ 0.5 %, candidate B = 0 %, candidate C ≈ 99 %, with 98.7 % of valid-water nighttime pixels being QC byte 65 (`M1/D0/E0/T1`, LST error ≤ 2 K). Requiring the strict `lst_error == 0` ("≤ 1 K") tier would leave Lake Balaton with no nighttime thermal product. **Mandatory transparency condition (binding):** every daily nighttime record also reports the coverage that the strict rule, candidate A, and candidate B would have produced, plus the QC-byte histogram over the QC-observed area; product metadata and the report state that the nighttime record rests on the "LST error ≤ 2 K" tier and that `M=0` "good quality" nighttime retrievals are effectively absent on Lake Balaton.
- **APPROVED (2026-09) — daytime acceptance rule:** the **same candidate-C rule applied symmetrically** to `terra_day` / `aqua_day` (one rule to document; C is a superset of any stricter rule, so it never discards good-quality daytime data). Daytime has not had an A/B/C comparison; an optional bounded daytime diagnostic could later confirm whether daytime can afford a stricter rule, but it is not required for the MVP.
- **APPROVED (2026-09) — daily confidence tier:** a daily observation is **never suppressed on coverage** (per `QA-001`); it carries a confidence flag from the accepted valid-water fraction `f`, three levels — `ok`, `low`, `none` (`f = 0`, the explicit no-valid-observation state). The exact `f` cut-off between `ok` and `low` (provisionally ~0.15) is calibrated in Phase 3–4 from the observed coverage distribution.
- **APPROVED (2026-09) — monthly minimum:** a monthly value is reported only when the month has **≥ 3** daily observations at `low` or better, per stream, per spatial unit; otherwise "insufficient valid observations", with the valid-day count and mean coverage still shown. The aggregation arithmetic itself is `METH-005`.
- QA-002 does not decide `METH-001…006`, the shoreline erosion treatment (recorded under `SPACE-001`: 0 m for the MVP), or `DATA-005/006`. It does not modify or relax any completed audit; the prototype's strict rule remains the documented engineering reference.
- **Rationale:** Acceptance thresholds determine when an observation or summary is sufficiently reliable to report; on Lake Balaton the nighttime QA rule is decisive — any rule requiring the strict LST-error tier discards essentially all nighttime data. The supervisor gave no methodological recommendation at the consultation, so the rule was chosen directly from the `AUDIT-011` + `AUDIT-012` evidence.
- **Evidence or source:** `PROJECT_CONTEXT.md` open decision 6; `docs/QA_002_PROPOSAL.md`; `AUDIT-011` (`docs/NIGHTTIME_GATE_ATTRIBUTION_DIAGNOSTIC_REPORT.md`); `AUDIT-012` (`docs/NIGHTTIME_QA_CANDIDATE_COMPARISON_REPORT.md`); `docs/FIRST_PROTOTYPE_SPECIFICATION.md` §7–8; `QA-001`; the 2026-09 supervisor consultation.
- **Approval provenance:** Nighttime acceptance rule approved by the user through the main coordinator after the 2026-09 supervisor consultation: "for 1 C accuracy we almost don't have data, so let's use the 2 C quality". The daytime rule, the confidence-tier principle, and the monthly minimum were approved in the same exchange by the user confirming the coordinator's recommendations ("for the QA-002 I say yes").
- **Affected files or components:** QA masks, acceptance logic, daily/monthly views, downloads, Phase 3 climatology/anomaly/percentile implementation.
- **Superseded decision:** None. Does not supersede or relax any completed audit or the prototype strict engineering rule (which remains the documented engineering reference).

### METH-005 — Monthly aggregation and missing streams

- **Date:** 2026-08-13
- **Status:** **approved (2026-09)** on the basis of `docs/METH_001_006_PROPOSAL.md` and the completed `AUDIT-013` sample-availability probe. Approval provenance: the user, after reviewing the recommendations and the `AUDIT-013` result and asking clarifying questions, confirmed "ok, everything looks good" and "ok, I understand let's proceed with the phases".
- **Decision:** Define monthly aggregation when Terra/Aqua streams or days are missing, including whether monthly summaries remain stream-specific.
- **Proposal (2026-09-02, not approved):** monthly summaries are **always stream-specific** (Terra/Aqua × day/night never merged). Per stream/month/whole-lake unit, computed only from daily observations that passed `QA-002` (tier `low` or better) and requiring **≥ 3** such days (the `QA-002` minimum): monthly mean temperature, monthly mean anomaly, hottest valid observation (date + value), maximum anomaly, and warm-observation count (days at ≥ 90th percentile). `< 3` qualifying days → "insufficient valid observations" with the count still shown. No zero-fill or interpolation (`QA-001`). The missing/cloud-obscured proportion is always reported.
- **Rationale:** Monthly statistics must not silently mix streams or handle missingness inconsistently.
- **Evidence or source:** `PROJECT_CONTEXT.md`, open decision 7.
- **Approval provenance:** Not approved.
- **Affected files or components:** Monthly summaries, missingness metrics, interface, downloads.
- **Superseded decision:** None.

### METH-006 — Standardized multi-sensor indicator

- **Date:** 2026-08-13
- **Status:** **approved (2026-09)** on the basis of `docs/METH_001_006_PROPOSAL.md` and the completed `AUDIT-013` sample-availability probe. Approval provenance: the user, after reviewing the recommendations and the `AUDIT-013` result and asking clarifying questions, confirmed "ok, everything looks good" and "ok, I understand let's proceed with the phases".
- **Decision:** Decide whether to create a standardized multi-sensor indicator. Direct averaging of the four observation streams remains excluded.
- **Proposal (2026-09-02, not approved):** **no** combined multi-sensor index for the MVP — the four streams sample the lake at different times and conditions. Instead the product shows the four streams side by side for the same date/month, each with its own anomaly, percentile, and label. A per-stream standardized anomaly (robust z-score), reported per stream and compared but never merged into one temperature, is a documented optional extension only.
- **Rationale:** Any combined indicator needs a defensible method that preserves stream differences.
- **Evidence or source:** `PROJECT_CONTEXT.md`, established stream separation and open decision 8.
- **Approval provenance:** Not approved.
- **Affected files or components:** Derived indicators, summaries, interface, documentation.
- **Superseded decision:** None.

### DATA-005 — Detailed Landsat method

- **Date:** 2026-08-13
- **Status:** proposed
- **Decision:** Define the exact Landsat role, date-selection rules, and shoreline/mixed-pixel treatment within the approved inspection role.
- **Rationale:** Reproducible hotspot inspection requires explicit selection and pixel-treatment rules.
- **Evidence or source:** `PROJECT_CONTEXT.md`, open decision 9.
- **Approval provenance:** Not approved.
- **Affected files or components:** Landsat preprocessing, hotspot maps, QA, documentation.
- **Superseded decision:** None.

### DATA-006 — ERA5-Land variables, aggregation, and latency

- **Date:** 2026-08-13
- **Status:** proposed
- **Decision:** Select ERA5-Land variables and temporal aggregation and define how data latency will be communicated.
- **Rationale:** Context variables and timing must be explicit and reproducible.
- **Evidence or source:** `PROJECT_CONTEXT.md`, open decision 10.
- **Approval provenance:** Not approved.
- **Affected files or components:** ERA5-Land processing, contextual views, metadata.
- **Superseded decision:** None.

### ARCH-001 — Precomputation and deployment strategy

- **Date:** 2026-08-13
- **Status:** proposed
- **Decision:** Define the precomputation asset/table schema, refresh cadence, ownership, storage/export destination, and GEE deployment strategy.
- **Rationale:** The approved performance principle requires an operational architecture before implementation or asset creation.
- **Evidence or source:** `PROJECT_CONTEXT.md`, “Performance strategy” and open decision 11.
- **Approval provenance:** Not approved.
- **Affected files or components:** GEE assets, tables, exports, refresh workflow, deployment.
- **Superseded decision:** None.

### VAL-001 — Validation acceptance criteria

- **Date:** 2026-08-13
- **Status:** proposed
- **Decision:** Define validation acceptance criteria and the response if in-situ data cannot be obtained.
- **Rationale:** Completion and scientific confidence require explicit acceptance rules.
- **Evidence or source:** `PROJECT_CONTEXT.md`, open decision 12.
- **Approval provenance:** Not approved.
- **Affected files or components:** Validation plan, tests, reports, release criteria.
- **Superseded decision:** None.

### SCOPE-003 — Six-week minimum viable outputs

- **Date:** 2026-08-13 (approved 2026-09)
- **Status:** **approved (2026-09).** Full proposal in `docs/SCOPE_003_PROPOSAL.md`. The user confirmed: "Both are extensions, let's try to finish first the core proposal, and then we can try to introduce ERA5-Land and then Landsat" — so ERA5-Land and Landsat are both **extensions**, ERA5-Land attempted first, only after the MVP core is delivered.
- **Decision:** Select minimum viable outputs for the six-week delivery period and distinguish them from optional extensions.
- **Proposal (2026-09-02, not approved):** **MVP core** = the MODIS anomaly product (four streams, lake-wide, daily observed LST + coverage/confidence flag + historical median reference + °C anomaly + Type-7 percentile + classification label, per the approved `QA-002` / `METH-*`), a daily observation view (numbers + accepted-pixel map + explicit no-observation state), a monthly summary view (per stream, ≥ 3 valid days), a deployed GEE application, and the written deliverables (documented code, technical report incl. the `AUDIT-013` summer-nighttime partial-coverage caveat, user guidance, presentation), with MVP-level validation (`VAL-001`: cross-stream consistency, Li et al. 2024 comparison, literature range, reproducibility; in-situ if obtained). **Extensions, only if the core is on track by ~week 4:** ERA5-Land context panel (`DATA-006`), Landsat hotspot inspection (`DATA-005`), a per-pixel anomaly map, a coarse west/centre/east daytime zonal split. **Documented future work:** formal four-basin products, littoral/pelagic zones, a Hobday-style consecutive-exceedance detector, any multi-sensor index beyond side-by-side display.
- **Rationale:** Delivery scope must match ~3 remaining weeks. The MODIS anomaly product plus the app plus the writing is the internship's core value and a realistic load; Landsat and ERA5-Land are each a fresh preprocessing pipeline.
- **Evidence or source:** `PROJECT_CONTEXT.md` open decision 13; `docs/SCOPE_003_PROPOSAL.md`; the consultation briefing; `AUDIT-013`.
- **Approval provenance:** Approved by the user through the main coordinator (2026-09): "Both are extensions, let's try to finish first the core proposal, and then we can try to introduce ERA5-Land and then Landsat, let's build phase 3".
- **Affected files or components:** Project plan, Phase 3–6 implementation scope, validation, documentation, presentation.
- **Superseded decision:** None.
